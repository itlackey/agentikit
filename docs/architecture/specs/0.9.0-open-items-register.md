# akm 0.9.0 — open-items register

Generated 2026-07-27 from an exhaustive re-scan of every known-issue source
after the three-phase cleanup program (commits `28e4b55..251d137`).

**Status of this document.** Three register scanners completed and verified
their items against the tree by reading code and running the CLI from source in
sandboxes. The independent re-verification pass, the completeness critic, and
the synthesis stage did NOT run — the session hit its token limit. So every
item below carries a *scanner* verdict that was tree-checked, but has not been
adversarially re-confirmed by a second agent. Treat severities as indicative.

Sources scanned: `0.9.0-docs-code-drift-register.md` (94 items),
`0.9.0-public-api-issue-backlog.md` (39), `0.9.0-release-surface-review.md`
(44 — this document had never been systematically worked through before).

NOT yet scanned: `0.9.0-decisions.md`, `tests/TESTS_REVIEW.md`, the unrouted
agent residuals, and the 38 untriaged Phase-3 review majors.

**"D-49 class"** marks an item whose file WAS edited by the program for a
different reason while the recorded defect went untouched — so the commit
history makes it look handled. 73 of the items below are that class.

## Owner rulings recorded but NOT executed (5)

These are the most serious findings: decisions the owner made that were written into the guardrails and then never dispatched as work.

### R-045 — Owner ruling 11 ("EXECUTE NOW") never executed — `instruction` still absent from PLACEMENT_SPECS

AGENT-GUARDRAILS.md ruling 11 mandates: a PLACEMENT_SPECS entry with an `instructions/` stash dir, an `instructions` row in matchers.ts, and a `placementTypes() ⊆ KNOWN_TYPES` subset assertion, explicitly superseding the older "do not add" note. NONE of the three landed. `src/core/asset/asset-placement.ts:89-166` — PLACEMENT_SPECS contains skill/command/agent/knowledge/workflow/script/memory/env/secret/lesson/task/session/fact; no `instruction`. `grep -n instruction src/indexer/walk/matchers.ts` → 0 hits. No subset assertion anywhere in src/ or tests/. `instruction` IS in KNOWN_TYPES (src/core/recognition-util.ts:105). asset-placement.ts has ZERO commits in 28e4b55..HEAD. The stated blocker (Q-18 show-routing collision) was resolved by `ownsPresentation`, so nothing was blocking it. No commit body in the 29-commit range mentions R-045.

Files: `src/core/asset/asset-placement.ts`, `src/core/recognition-util.ts`, `src/indexer/walk/matchers.ts`

### R-039 — Owner ruling 9 (semantic split-default) never implemented — bare install still pulls the ~130 MB model

Ruling 9 requires flipping the RUNTIME default to `semanticSearchMode: "off"` while the wizard pre-selects it ON. Tree still defaults to "auto" in BOTH places: `src/core/config/config.ts:108` (`semanticSearchMode: "auto"`) and `src/core/config/config-schema.ts:135` (`z.enum(["off","auto"]).default("auto")`). config-schema.ts has ZERO commits in range. Reproduced: a fresh `akm init` in a sandbox wrote `"semanticSearchMode": "auto"` to config.json. The wizard half also cannot work as ruled: `src/setup/steps/semantic.ts:27` derives `initialValue: current.semanticSearchMode !== "off"`, so flipping the default to "off" would flip the wizard to pre-selected OFF — the ruling needs BOTH edits and neither happened. (The download-size warning text at src/setup/semantic-assets.ts:36-47 already existed.) No commit body mentions R-039.

Files: `src/core/config/config.ts`, `src/core/config/config-schema.ts`, `src/setup/steps/semantic.ts`

### Ruling 12 — Owner ruling 12 ("take the breaking changes NOW in 0.9.x") is 1-of-5 implemented **[D-49 class]**

Ruling 12 lists five breaking changes to land now, each with a CHANGELOG migration note. Only ONE landed. (1) unknown-command exit 1→2 — DONE (96ff2fe, CHANGELOG:81). (2) drop dead `installedKitCount` — OPEN: still in the public envelope at src/sources/types.ts:205,287,322 and populated at installed-stashes.ts:273,307,338 and source-add.ts:117,177,275. (3) drop dead `graphPath` — OPEN: src/indexer/db/graph-db.ts:444,575 and src/commands/graph/graph.ts:32,45,55,65,75,88 (D-49: graph-db.ts WAS edited by 23119f5, which deleted R-063 #4's dead functions from the same file). (4) rename the persisted `save` event type — OPEN: src/commands/sources/sources-cli.ts:164 `eventType: "save"` and :171 `output("save", result)` (D-49: sources-cli.ts edited by 4d82df1 AND 0134501). (5) update/upgrade/sync verb renames and one canonical bare-group behaviour — OPEN, see R-049/R-053. No commit body mentions items 2-5.

Files: `src/sources/types.ts`, `src/commands/sources/installed-stashes.ts`, `src/indexer/db/graph-db.ts`, `src/commands/sources/sources-cli.ts`

### Q-18 — instruction still absent from PLACEMENT_SPECS — contradicts owner ruling 11 (EXECUTE NOW)

AGENT-GUARDRAILS ruling 11 explicitly superseded guardrail 8 and ordered: a PLACEMENT_SPECS entry with an instructions/ stash dir, an instructions row in matchers.ts, and a placementTypes() subset assertion. None exist. asset-placement.ts:89-166 has 14 keys, no `instruction`; zero 'instruction' hits in src/indexer/walk/matchers.ts; no subset assertion in src/ or tests/. `instruction` IS in KNOWN_TYPES (recognition-util.ts:104) and TYPE_PRESENTATION (type-presentation.ts:128) — the half-registered state the item describes. Verified live: `akm info --format=json` assetTypes lists 13 types, no instruction; `akm init` creates no instructions/ dir. The stated blocker (ownsPresentation) was resolved. asset-placement.ts was NOT touched by the program.

Files: `src/core/asset/asset-placement.ts`, `src/core/recognition-util.ts`, `src/indexer/walk/matchers.ts`

### X-RULING-9 — Owner ruling 9 (R-039 semantic split default) has ZERO implementation — a bare install still silently pulls the ~130 MB model

AGENT-GUARDRAILS.md ruling 9 is explicit: 'Flip the *runtime* default to `semanticSearchMode: "off"` so a bare install never silently pulls the ~130 MB model. BUT in the interactive `akm setup` wizard, the semantic option is pre-selected ON.' Tree: `src/core/config/config.ts:108` is still `semanticSearchMode: "auto"` and `src/core/config/config-schema.ts:135` is still `z.enum(["off","auto"]).default("auto")`. Neither half landed. No commit in 28e4b55..HEAD mentions R-039 or semanticSearchMode (grepped all 29 commit bodies). Worse, the wizard's pre-selection at `src/setup/steps/semantic.ts:27` is DERIVED from the config default (`initialValue: current.semanticSearchMode !== "off"`), so whoever flips the default will silently flip the wizard OFF too and break ruling 9's second half in the same edit. This register does not own R-039 (0.9.0-public-api-issue-backlog.md:404 does), but it is the reason A2's headline symptom is still live rather than moot.

Files: `src/core/config/config.ts`, `src/core/config/config-schema.ts`, `src/setup/steps/semantic.ts`


## Needs an owner decision (6)

### R-038 — Q-14 ruling half-implemented — cli.md gained an `### extract` section with NO privacy or LLM-transmission disclosure **[D-49 class]**

The ruling: "Land the first-run consent and prominent-disclosure requirements in that new STABILITY.md tier entry and reference section". Commit fadc65e added `docs/reference/cli.md:1952-1982`, but it reads only "Extract durable insights from native coding-agent session files (claude-code, opencode) and queue them as proposals" — it never says the command reads OTHER TOOLS' PRIVATE SESSION TRANSCRIPTS and SENDS THEIR CONTENT TO THE CONFIGURED LLM, which is the entire substance of R-038. STABILITY.md:139 tiers `extract` Experimental but carries no disclosure either. `grep -rn 'consent|first-run|firstRun'` across the extract sources → 0 hits. D-49 shape: the file was touched FOR this item, the item's substance was not.

Files: `docs/reference/cli.md`, `STABILITY.md`, `src/improve/extract.ts`

### R-036 — Q-05/Q-14 ruling residual — scheduled `akm improve` entries still sit outside the autonomy gate

TRIAGE-VERIFIED correctly reclassifies R-036-as-filed NOT-A-DEFECT (`akm setup` installs no scheduler entries), which cancels the wizard-step half. But the ruling's other half — "The scheduled `improve` entries must sit behind that off-by-default gate" — is unimplemented: `src/commands/tasks/default-tasks.ts:197-205` `registerDefaultTasks` guards only on `isCiEnvironment()`; there is no `isImproveAutonomyEnabled` check anywhere in default-tasks.ts (the only autonomy references are in tasks.ts:717-749, which merely REPORTS the gate in `tasks doctor`). So `akm tasks init` still installs hourly/4-hourly/nightly `akm improve` crons (default-tasks.ts:51-92) with the autonomy gate off. Blast radius is much smaller than filed (explicit opt-in command, not setup) — owner should confirm whether the ruling half is still wanted.

Files: `src/commands/tasks/default-tasks.ts`, `src/commands/tasks/tasks.ts`

### R-010 / R-011(full) / R-016 / R-042 / R-044 / R-059 — Six deliberate deferrals — confirmed untouched, listed so the enumeration is complete

All confirmed unchanged at HEAD and all named as deliberate deferrals in the triage. R-010: src/integrations/lockfile.ts still consumes only `localRoot`; R-012's corruption fix landed but no frozen-install/integrity-recheck path exists. R-011 full: src/sources/providers/tar-utils.ts:40-41 still early-returns for git/github ("Content integrity cannot be verified from a commit hash"); only the S carve-out (post-clone HEAD compare) landed in 6c3f33e. R-016: src/setup/setup.ts:784-786 `runSetupWithDefaults` still creates its context with `nonInteractive: true` and hardcodes `online: false`. R-042: only the workflow native engine consumes the harness result extractors (src/integrations/agent/builder-shared.ts:97 says so explicitly); the stale "NOT registered anywhere" comment half WAS fixed by 24f9a81 (only two deliberate "NOT registered" comments remain, codex/index.ts:48 and copilot/index.ts:45). R-044: `grep -n 'secrets/' src/core/config/config.ts` → 0 hits; config still cannot reference managed secrets. R-059: both `getRegistryCacheDir` (paths.ts:289) and `getRegistryIndexCacheDir` (paths.ts:293) still exist as parallel schemes. Owner should confirm these six stay deferred past 0.9.0.

Files: `src/integrations/lockfile.ts`, `src/sources/providers/tar-utils.ts`, `src/setup/setup.ts`, `src/core/config/config.ts`, `src/core/paths.ts`

### D-63 — Migration TL;DR still teaches `akm env run env:<name>`

v0.8-to-v0.9.md:232 still teaches the colon spelling; docs/posts/akm-command-tour.md:296-297 likewise (`akm show env:production`, `akm env run env:production -- env`). Verified live these now hard-fail with INVALID_FLAG_VALUE. WAVE2-CARRYOVER listed both files as 'deliberately deferred to Phase 3 docs sweep, do not lose'; 24f9a81 instead reclassified docs/migration and docs/posts as chartered-historical and kept them. That is a reversal of a recorded routing decision and needs an owner call, not a silent keep.

Files: `docs/migration/v0.8-to-v0.9.md`, `docs/posts/akm-command-tour.md`

### NEW-E — E-2 silent unknown-flag acceptance left as an explicit open owner decision, now with a documented instance

e6ea2ae's body: 'The underlying cause is general — citty silently accepts unknown flags on every command — but that is a wide-blast-radius behavior change and is left for an explicit decision rather than fixed here.' It was left with no ticket. D-38 is now a concrete instance shipped in the docs: `akm log list --limit 20` is documented, exits 0, and silently ignores the limit — the same silent-wrong-answer shape 4d82df1 called the worst kind of defect (B-5).

Files: `src/commands/observability-cli.ts`, `docs/reference/data-and-telemetry.md`

### X-PHASE3-MAJORS — 38 'major' Phase-3 review findings remain untriaged — the sweep is not fully reviewed

Carried forward, not resolved by anything I found in the tree. WAVE2-CARRYOVER.md:74-77 records: '38 "major" review findings across the 12 Phase-3 packages are UNTRIAGED. The blockers were fixed; the majors were not individually verified. Full review payload: /tmp/claude-0/.../tasks/wichmt95i.output. Do not treat the Phase-3 sweep as fully reviewed until these are worked through.' Task #17 ('Phase 3 — end-to-end docs/comments/help sweep') is still in_progress. Several of the confirmed findings above (C8's two surviving tracker labels, the cli.md workflow table, D-49) are the kind of item that population would contain, which suggests the 38 are a live reservoir rather than a formality. Flagging for routing since nothing in 28e4b55..HEAD discharges it.


## Register contradicted by the tree (4)

The register or a commit body claims something the code refutes. These make the existing documents actively misleading.

### R-067 — Register-contradicting fix: commit claims R-067 but all THREE sites the register names still process.exit() **[D-49 class]**

Commit 96ff2fe's body: "R-067 — emitJsonError process.exit()ed on the error path of every command, bypassing cleanup. It now sets process.exitCode and returns." That is a DIFFERENT site from the item. R-067 as filed names `env run` (env-cli.ts:305), `secret run` (secret-cli.ts:257), and `runMigrationTool` — all three are unchanged at HEAD: `src/commands/env/env-cli.ts:305` `process.exit(result.status ?? 0)`, `src/commands/env/secret-cli.ts:257` `process.exit(result.status ?? 0)`, `src/commands/migration-tool.ts:36` `if (result.status !== 0) process.exit(result.status ?? 1)`. All three still skip the `disposeDispatchResources()` finally in cli.ts. D-49 compounding: 290beb9 edited BOTH env-cli.ts and secret-cli.ts for Q-08 while these lines sat untouched. R-067 reads as closed in the commit log but is open as filed.

Files: `src/commands/env/env-cli.ts`, `src/commands/env/secret-cli.ts`, `src/commands/migration-tool.ts`

### Q-17 — runtime-boundary-design.md: ruled retired+consolidated, actually kept with a banner that reverses the ruling **[D-49 class]**

Ruling: consolidate into architecture.md (Module Boundaries / Tech Stack), DROP the 'Architect: investigate' checklists and the 3-hard-import-files claim 'rather than carrying them over', remove the row from docs/architecture/README.md, retire the file. 24f9a81 instead prepended an 'Implementation note (added later)' banner ending 'The checklist items below are kept as the historical design record' — an explicit reversal. All three 'Architect: investigate and fill in:' blocks survive at :45, :82, :117 with unchecked boxes. docs/architecture/README.md:8 still links the file. architecture.md's Module Boundaries (:457) and Tech Stack (:508) carry no bun:sqlite/Bun.* import-boundary rule and no database.ts/runtime.ts export table.

Files: `docs/architecture/runtime-boundary-design.md`, `docs/architecture/README.md`, `docs/architecture/architecture.md`

### D-03 — mv tier entry present but carries a claim TRIAGE-VERIFIED already proved false **[D-49 class]**

STABILITY.md:73-76 states mv's 'inbound-ref rewriting targets bare conceptIds rather than the anchored `bundle//conceptId` prose form, so it can rewrite non-refs while leaving real refs dangling'. TRIAGE-VERIFIED item 21 flags this FALSE. Confirmed: buildRewritePatterns (mv-cli.ts:150-171) returns four patterns, two built from `qualifiedCore = ${bundleId}//${fromConcept}`. 4d82df1 fixed the IDENTICAL claim in cli-hints-full.md ('matches no fully-qualified bundle// form at all... That was false') and verified it by experiment, but left it in the release contract. STABILITY.md was touched by fadc65e and 05537cf.

Files: `STABILITY.md`, `src/commands/mv-cli.ts`

### E6 — Setup schedules — the item's factual premise is refuted by the tree

PREMISE FALSE. E6 opens 'Today interactive setup installs OS scheduler entries (hourly + 4-hourly LLM-calling improve jobs) with only a "server install?" question, while `--yes` installs none — materially different installs.' TRIAGE-VERIFIED (which is authoritative over the registers) reclassified this as R-036 NOT-A-DEFECT: `akm setup` installs NO scheduler entries — `stepScheduledTasks()` returns early on nonInteractive/CI, the multiselect's `initialValues` is empty on a fresh install, and `DEFAULT_IMPROVE_TASKS` registers only via an explicit `akm tasks init`. The tree agrees: `src/setup/setup.ts:665-666` reaches `stepScheduledTasks()` (defined `src/setup/steps/tasks.ts:161`) only on the interactive path and the step 'reviews the plan and asks one explicit confirmation before changing task files or scheduler state' (:663-665). Since neither path installs anything by default, the interactive-vs-`--yes` divergence E6 is built on does not exist. The residual — whether setup SHOULD offer the review-first task set — is a product question, not a defect.

Files: `src/setup/setup.ts`, `src/setup/steps/tasks.ts`


## Confirmed open (114)

### R-027 — D-49: `secret path` and `secret remove` still resolve to DIFFERENT stashes — destructive, reproduced at HEAD **[D-49 class]**

REPRODUCED in a sandbox at HEAD: with stashA (working) and stashB (defaultWriteTarget), both holding `secrets/shared`, `akm secret path secrets/shared` printed `.../stashA/secrets/shared` while `akm secret remove secrets/shared --yes` returned `{"ref":"stashb//secrets/shared","removed":true}` and DELETED stashB's file. The read path uses `findEnvSource` (all sources, src/core/env-secret-ref.ts:90); mutations use `resolveMutationTarget` (:240, :276). The divergence is documented in-file at :203-212 but never reconciled. D-49 pattern: commit 290beb9 edited THIS EXACT FILE for the Q-08 colon-ref rejection (+37/-8 lines), touching lines immediately adjacent to the two resolvers, while R-027 — a P1 destructive defect the register cites at these very lines — was never in that package's brief and was left untouched. The file now shows as "handled" in a commit.

Files: `src/core/env-secret-ref.ts`

### R-065 #6 — D-49 (worst instance): the Phase-3 COMMENT SWEEP edited reflect.ts and left the exact recorded comment defect at line 6 **[D-49 class]**

R-065 #6 names `reflect.ts:6` / `distill.ts` / `consolidate.ts` headers documenting commands that were never registered. Verified unregistered at HEAD: `akm reflect`, `akm distill`, `akm consolidate` all exit 2. Headers still assert them as CLI commands: `src/commands/improve/reflect.ts:6` ("`akm reflect [ref]` — proposal-producing agent command (#226)") and `src/commands/improve/distill.ts:6`. Commit 24f9a81 — the comment sweep itself, whose body claims "~30 docblocks cited files that no longer exist" were fixed — DID edit reflect.ts (9 lines: it corrected a stale `src/commands/propose.ts:163-178` citation mid-file) and left line 6 alone. TWO further sites the register never listed: `src/output/shapes/distill.ts:5` and `src/output/text/distill.ts:5`. Compounding: commit e34452a's own body states "`akm reflect` and `akm distill` … are internal improve-pipeline process names, not commands" while fixing the user-facing string — the program knew, fixed one surface, and left the recorded one.

Files: `src/commands/improve/reflect.ts`, `src/commands/improve/distill.ts`, `src/output/shapes/distill.ts`, `src/output/text/distill.ts`

### R-063 #9 — Q-02 sweep was explicitly told to delete curate's colon regexes and did not — `preferBroadRootRepresentative` is now provably dead code **[D-49 class]**

The register's Q-02 note explicitly routes this: "curate's colon-grammar family regexes (curate.ts:769,779) are now part of the mandated colon-removal sweep — schedule their deletion with the Q-02 work". Commit 0134501 IS the Q-02 sweep and claims "Four sites beyond the register's list turned up while sweeping" — yet curate.ts has ZERO commits in 28e4b55..HEAD. Still present: `src/commands/read/curate.ts:769` `/^knowledge:skills\/(.+?)\/references\/(.+)$/.exec(first.ref)` and `:779` `` const rootRef = `skill:${match[1]}` ``. PROVEN DEAD by experiment: I indexed a sandbox stash with `skills/testing/SKILL.md` and `knowledge/skills/testing/references/api.md`; search returned refs `'skills/testing'` and `'knowledge/skills/testing/references/api'` — slash conceptIds, so the colon regex can never match. The sibling function `getCurateFamily` (:601-620) WAS converted to the slash grammar (its comment says "F4b: `ref` is a search-hit ref in the 0.9.0 conceptId grammar"), which is what makes the omission visible. Consequence: the whole "prefer the broad root skill over its reference page" curate behaviour (called live at :259) is silently disabled. Also unswept in the same file: `:424` `` `${item.type}:${item.name}` `` emits colon refs into the user-visible curate summary, and its own comment claims it emits `registry:<name>`, contradicting the code.

Files: `src/commands/read/curate.ts`

### R-009 — D-49: `akm search` still rejects the empty query its own help advertises **[D-49 class]**

REPRODUCED: `akm search --format json` → `{"ok":false,"error":"A search query is required...","code":"MISSING_REQUIRED_ARGUMENT"}`. Help at `src/commands/read/search-cli.ts:36` still opens with 'Search query (omit to list all assets)'; the handler throws at `:71-77`. D-49 pattern: search-cli.ts was edited TWICE in range — 24f9a81 (docs sweep; it rewrote the neighbouring `--include-proposed` description on line 54) and e6ea2ae (added rejectRemovedScopeFlag) — neither touched the contradiction two lines above. No commit body mentions R-009.

Files: `src/commands/read/search-cli.ts`

### R-068 #6 — D-49: `--belief` help still says "Memory belief filter" while the code documents it applies to ANY entry **[D-49 class]**

`src/commands/read/search-cli.ts:60`: "Memory belief filter: all|current|historical. current keeps active memory beliefs; historical keeps contradicted/superseded/archived memory beliefs." Contradicted in-tree by `src/indexer/search/db-search.ts:796-797`: "the belief filter applies to ANY flagged entry, not just memories, so `current`/`historical` filters catch contradicted/superseded KNOWLEDGE too" — and `matchBeliefFilter` (:794-811) has no type predicate. Same D-49 file as R-009: two commits edited search-cli.ts for other reasons. Commit 251d137 fixed a DIFFERENT `--belief` claim (concepts.md's default value) in the same program without touching the help string the register actually names.

Files: `src/commands/read/search-cli.ts`, `src/indexer/search/db-search.ts`

### R-065 #1 + R-068 #1 — D-49: init.ts edited by the Q-02 sweep; both recorded defects in it left untouched **[D-49 class]**

Commit 0134501 edited `src/commands/sources/init.ts` (per its body: "the clone/proposal/init help examples (E-07)"). Both recorded defects survive. R-065 #1: the module docblock at `init.ts:9` still says "and ensures ripgrep is available" — `grep -rn ripgrep src/ --include=*.ts` outside this file returns ZERO hits; nothing uses ripgrep. R-068 #1: `init.ts:187` still runs `spawnSync("git", ["init", dir], {...})` with no exit-status check (the comment calls it "Non-fatal", but the register's ask was to check it). The Phase-3 comment sweep (24f9a81) did not touch init.ts at all despite claiming to sweep stale docblocks repo-wide.

Files: `src/commands/sources/init.ts`

### R-020 — `akm show` still drops the canonical `ref`, and Phase 3 DOCUMENTED the defect as correct behaviour **[D-49 class]**

REPRODUCED: `akm show knowledge/guide --format json` (default) returns keys ['action','content','editable','name','origin','path','related','type'] — NO `ref`. `--detail brief` returns ['action','description','editable','name','origin','path','type'] — also no `ref`. `src/output/shapes/helpers.ts:453-467` (summary shape) and `:469-495` (default base) both omit "ref" from pickFields. show.ts was edited by 4d82df1 and 0134501 for other reasons. Worse than a plain miss: commit 251d137 wrote the defect into the docs as intended behaviour — `docs/guides/search-discovery.md:46-49` now reads "It is dropped at `--detail normal` (the `human`-shaped \"normal\" hit has no `ref` field)". The register item is open; the docs now cement it. Agents still get no follow-up identifier at the default detail level.

Files: `src/output/shapes/helpers.ts`, `src/commands/read/show.ts`, `docs/guides/search-discovery.md`

### R-005 — Q-01 ruling half-implemented: STABILITY.md got the mv tier entry, the shipped conventions fact still teaches the opposite **[D-49 class]**

The Q-01 ruling is two-part: "rewrite `facts/conventions/organization.md` and the other D3-side docs to teach `mv`, and add the missing Experimental tier entry for `mv` to STABILITY.md". Half two landed (STABILITY.md:72-73 and :381-382 both cover `akm mv` as Experimental). Half one did NOT: `src/assets/stash-skeleton/facts/conventions/organization.md` has ZERO commits in 28e4b55..HEAD and still reads "**A rename is delete plus create.** There is no command that preserves an asset's identity or learned state across one", followed by the hand-rolled `mv <stash>/memories/old-note.md ...` + `grep the stash` procedure. This file ships into every user stash via `copyStashSkeleton`, so every scaffolded stash still tells agents to avoid `akm mv`. Compounding: commit 4d82df1 fixed the hints corpus's `akm mv` claim in the same program without touching the scaffolded fact.

Files: `src/assets/stash-skeleton/facts/conventions/organization.md`, `STABILITY.md`

### R-053 — Bare-group behaviour still inconsistent — ruling 12's "one canonical" never picked

Measured at HEAD in a sandbox: `akm log` exit 2, `akm lessons` exit 2, `akm migrate` exit 2, but `akm env` exit 0, `akm config` exit 0, `akm proposal` exit 0, `akm tasks` exit 0 (default action), `akm graph` exit 1 (default action, data-not-found). Still two-to-three conventions. The register's own description is stale in a third way (it says log/lessons exit 0 — 96ff2fe moved them to 2, which is a side effect of R-032, not a decision on this item). Ruling 12 mandated picking one canonical behaviour; nothing did.

Files: `src/cli.ts`

### R-049 — `update`/`upgrade`/`sync` verbs unrenamed; `sync` still emits the retired `save` event name **[D-49 class]**

Ruling 12 explicitly lists "the `update`/`upgrade`/`sync` verb renames" as take-now. No rename landed. The narrowest concrete half — the retired name leaking into the persisted event stream — is verbatim unchanged: `src/commands/sources/sources-cli.ts:164` `eventType: "save"` and `:171` `output("save", result)`. D-49: sources-cli.ts was edited by 4d82df1 (help/description fixes) and 0134501 (colon sweep) without touching either line.

Files: `src/commands/sources/sources-cli.ts`

### R-021 — `--detail brief` as a flag and as a config default still produce two different payloads

REPRODUCED: `akm show knowledge/guide --detail brief --format json` → keys ['action','description','editable','name','origin','path','type']. `akm config set output.detail brief` then `akm show knowledge/guide --format json` → ['action','content','editable','name','origin','path','related','type']. Same nominal level, two payloads (flag drops `content`/`related` and adds `description`; config does the opposite). Root cause unchanged: `src/commands/read/search-cli.ts:261` still detects only the literal CLI token via `invocation.getFlagValue("--detail")` to select `buildBriefResponse` (src/commands/read/show.ts:469-470); the config value reaches only the shaper. No commit body mentions R-021. Note commit 96ff2fe addressed the DIFFERENT R-050(b) claim about brief==normal on show, which may explain why R-021 looked handled.

Files: `src/commands/read/search-cli.ts`, `src/commands/read/show.ts`

### R-013 — `akm add --provider npm` still silently creates a filesystem bundle

REPRODUCED at HEAD: `akm add lodash --provider npm` wrote to config.json a bundle `"lodash"` with `"path": "/home/user/akm/lodash"` — a filesystem path resolved against CWD, not an npm package. `providerType` is still honoured only for remote-URL targets (src/commands/sources/source-manage.ts). No commit in 28e4b55..HEAD mentions R-013; source-manage.ts has zero commits in range.

Files: `src/commands/sources/source-manage.ts`, `src/sources/config-sources.ts`

### R-014 — `akm add --provider` text output still prints "Installed undefined"

REPRODUCED verbatim at HEAD: `akm add lodash --provider npm --format text` printed `Installed undefined (0 directories scanned, 0 total assets indexed)`. The two add paths still return incompatible shapes and the shared renderer reads the wrong one — `src/output/text/command-format.ts:510-514`. command-format.ts has zero commits in range and no commit body mentions R-014.

Files: `src/output/text/command-format.ts`, `src/commands/sources/source-manage.ts`

### R-018 — `akm curate --type` still bypasses the entire curation algorithm

`src/commands/read/curate.ts:255-256`: `if (selectedType && selectedType !== "any") { selectedStashHits = stashHits.slice(0, limit); }` — the else branch (:258-262) is the only path that runs `selectCuratedStashHits` (family collapse, intent nudges, score floor) and `preferBroadRootRepresentative`. Two qualitatively different products under one command name, unchanged. curate.ts has zero commits in range.

Files: `src/commands/read/curate.ts`

### R-019 — curate still hard-caps registry results at 2 regardless of --limit

`src/commands/read/curate.ts:265-266`: `registryHits.slice(0, Math.min(2, limit - selectedStashHits.length))`. The literal 2 is unchanged. curate.ts has zero commits in range; no commit body mentions R-019.

Files: `src/commands/read/curate.ts`

### R-015 — `akm update --all` still silently skips plain (non-lock-backed) sources with no report

`src/commands/sources/installed-stashes.ts:513-521`: `selectManagedTargets(...)` does `if (all) return installs;` where `installs` is the managed (lock-backed) set only. No skip counter, no `skipped` field, no warning: `grep -n 'skipped' src/commands/sources/installed-stashes.ts` returns only the unrelated comment at :530. The Q-09 note made this skip report "the sole disclosure" of the managed/plain split — so nothing discloses it. installed-stashes.ts has zero commits in range.

Files: `src/commands/sources/installed-stashes.ts`

### R-055 — Reads-are-writes still undisclosed and has no opt-out

`akm search`'s description is still the bare "Search the stash" (src/commands/read/search-cli.ts:31) — no statement that a search appends events, inserts usage rows, and bumps EMA utility. `grep -rn 'no-log|noLog|--no-track' src/cli.ts src/commands/read/search-cli.ts` → 0 hits; the internal `skipLogging` (9 src occurrences) is still never exposed as a flag. No commit body mentions R-055.

Files: `src/commands/read/search-cli.ts`, `src/indexer/search/search.ts`

### R-057 — `akm info` still omits stashDir/defaultBundle/per-type counts and still bypasses --quiet

`src/commands/sources/info.ts:64-78` — the returned object is {schemaVersion, version, assetTypes, searchModes, semanticSearch, registries, sourceProviders, indexStats}: no `stashDir`, no `defaultBundle`, no per-type asset counts. `info.ts:103` still writes raw `process.stderr.write('[akm info] failed to read index stats...')`, bypassing --quiet. The comment at :58-60 claims it avoids "a bare getDbPath() call" while :61 is `options?.dbPath ?? getDbPath()` — the R-065 #2 self-contradiction, also unchanged. info.ts has zero commits in range.

Files: `src/commands/sources/info.ts`

### R-058 — remove/update destructive asymmetry unchanged — `update` still rm -rf's without confirmation

`src/commands/sources/installed-stashes.ts:421-428`: `cleanupDirectoryBestEffort(managed.localRoot)` runs unconditionally on the update path with no prompt, while `akm remove` still confirms. The mirror-cache-left-behind and orphaned-`akm clone`-download halves are likewise untouched. installed-stashes.ts has zero commits in range; no commit body mentions R-058.

Files: `src/commands/sources/installed-stashes.ts`

### R-048 — npm description still calls akm "A package manager"

`package.json:5`: "akm (Agent Knowledge Management) — A package manager for AI agent skills, commands, tools, and knowledge. …". package.json has zero commits in 28e4b55..HEAD. This is the register's cheapest item and directly contradicts the toolkit framing the whole backlog is written under. No commit body mentions R-048.

Files: `package.json`

### R-064 #1 — `IndexOptions.reEnrich` still threaded three layers though the flag is removed and every caller passes false **[D-49 class]**

The flag is confirmed REMOVED — `src/commands/sources/stash-cli.ts:124-126` explicitly throws "`akm index --re-enrich` has been removed". Yet the parameter survives end-to-end (src/indexer/indexer.ts:190,312,362,680,737,1680,1697-1698,1795,2170,2204; src/indexer/walk/index-context.ts:70) and all three internal callers hardcode false (graph.ts:653, loop-stages.ts:1130, loop-stages.ts:1275). Permanently dead. D-49: indexer.ts was edited by THREE commits in range (fa6df0c, 930d380, 9e74419) for other reasons.

Files: `src/indexer/indexer.ts`, `src/indexer/walk/index-context.ts`

### R-064 #2 — `IndexResponse.graphQuality` declared and never assigned **[D-49 class]**

`src/indexer/indexer.ts:137` declares `graphQuality?: {...}`; it is the ONLY occurrence of the identifier in all of src/ (`grep -rn graphQuality src/ --include=*.ts` → 1 hit). Never assigned, never read. TRIAGE-VERIFIED calls this "clean OPEN". D-49: indexer.ts edited three times in range.

Files: `src/indexer/indexer.ts`

### R-064 #4 — D-49: `durationMs` still computed then void-ed in improve-cli.ts **[D-49 class]**

`src/commands/improve/improve-cli.ts:349` `const durationMs = Date.now() - startedAtMs;` … `:391-392` `// durationMs reserved for future use (no console emission today).` / `void durationMs;`. Unchanged. D-49: improve-cli.ts WAS edited by 24f9a81 (the docs/comment sweep) for other reasons.

Files: `src/commands/improve/improve-cli.ts`

### R-064 #5 — Dead `updatable` READ survives in the text renderer

Per TRIAGE-VERIFIED the item is inverted: `updatable` is not on `SourceEntry` at all, and the survivor is a dead READ. Confirmed at HEAD: `grep -rn updatable src/ --include=*.ts` returns exactly ONE hit — `src/output/text/command-format.ts:500` `if (src.updatable === true) flags.push("updatable")` — a condition nothing can ever satisfy. command-format.ts has zero commits in range.

Files: `src/output/text/command-format.ts`

### R-063 #2/#3/#5/#6/#8/#11 — Six confirmed-dead R-063 sub-items untouched — only #4 was packaged

Only #4 landed (23119f5 deleted loadGraphMetaOnly/loadGraphEntitiesByPath). #1 and #7 are correctly cancelled by TRIAGE-VERIFIED (both reachable), #10 correctly cancelled (deliberate). The rest are unchanged and verified src-dead at HEAD: #2 `injectIntoEnv` (src/commands/env/env.ts:115 — sole src occurrence besides its own docstring); #3 secret `listNames` (src/commands/env/secret.ts:96 — zero other src callers); #5 `parseConfigValue` (config-cli.ts:44, zero src callers) plus the unreachable `toggleComponent` throw (config-cli.ts:130, whose own comment says "this is unreachable") plus the dead group-level `--list` boolean (config-cli.ts:136, shadowed by the real `list` subcommand at :166); #6 `listStashes`/`SourceListResult` (source-manage.ts:36,157 — zero other src references); #8 the `semanticSearch` projection branch (output/shapes/helpers.ts:290); #11 `setup.taskSchedules` (src/core/config/schema/setup.ts:29 — the only src reference, plus one negative test assertion). No commit body mentions any of them.

Files: `src/commands/env/env.ts`, `src/commands/env/secret.ts`, `src/commands/config-cli.ts`, `src/commands/sources/source-manage.ts`, `src/output/shapes/helpers.ts`, `src/core/config/schema/setup.ts`

### R-065 #5 — All four (actually seven) TODO(R2) markers still describe implemented features as unimplemented

`grep -rn 'TODO(R2)' src/ --include=*.ts` → SEVEN hits, none removed: workflows/program/parser.ts:700 (max_loops "is engine" rework), workflows/program/schema.ts:123,153,175 (enforcement/gates/schema validation "lands with the engine rework"), workflows/ir/compile.ts:187,224,244 (maxLoops, reducer schema validation, retry dispatch). The register says all are implemented in native-executor.ts; the engine is confirmed present (src/workflows/exec/native-executor.ts, scheduler.ts, worktree.ts, watch.ts, unit-writer.ts, `workflow_run_units` + migration 004). Neither the Phase-3 comment sweep (24f9a81) nor the Q-05 gate commit (05537cf), which both worked in this area, touched them.

Files: `src/workflows/program/parser.ts`, `src/workflows/program/schema.ts`, `src/workflows/ir/compile.ts`

### R-065 #4 — `akm history` docblock still names `events.jsonl` as an event source; the store is state.db

`src/commands/sources/history.ts:12` — "`events.jsonl` append-only stream (opt-in via `--include-proposals`)" — plus five more in-file references at :74,:95,:117,:150,:170. Directly contradicted in-tree by `src/core/events.ts:10-11`: "Events are stored in the `events` table in `state.db` (SQLite, WAL mode) instead of a flat `events.jsonl` file" and `:203` "Events are written exclusively to the `events` table in `state.db`". history.ts has zero commits in range despite the Phase-3 comment sweep.

Files: `src/commands/sources/history.ts`, `src/core/events.ts`

### R-065 #2 — `akm info` comment still asserts it avoids the `getDbPath()` call it makes

`src/commands/sources/info.ts:58-60`: "resolve the DB path from config so info reads the same database that health and search use, rather than a bare getDbPath() call that ignores XDG_DATA_HOME or per-config overrides" — immediately followed by `:61` `const resolvedDbPath = options?.dbPath ?? getDbPath();`. The `options?.dbPath` seam is test-only, so production takes the bare call the comment disclaims. info.ts has zero commits in range.

Files: `src/commands/sources/info.ts`

### R-066 #1 — Two near-identical secret-directory walkers still coexist **[D-49 class]**

`src/commands/env/secret.ts:96-115` (`listNames`) and `src/commands/env/secret-cli.ts:50-74` (`listSecretsRecursive`) still share the same recursive walk, the same `.lock`/`.sensitive` exclusions, and the same sibling-`.sensitive` suppression. #2/#3/#4/#5 all landed (23119f5, fa6df0c, 6c3f33e); #1 did not. Note the duplicate is also R-063 #3's dead function, so one deletion closes both. D-49: secret-cli.ts was edited by 290beb9 for Q-08.

Files: `src/commands/env/secret.ts`, `src/commands/env/secret-cli.ts`

### D-49 — D-49 itself confirmed still open — the workflows guide still describes the pre-engine world **[D-49 class]**

`docs/guides/claude-code-vs-akm-workflows.md` was edited by 24f9a81 (the prose-in-fence fix) and its substance left. Still present: `:238` "**akm does not execute workflow steps.**"; `:164` "An akm workflow **is a document**, not a program."; `:11` "sequential, human-in-the-loop run-state tracker"; `:257` "**Strict sequentiality.** There is exactly one `current_step_id`"; `:345` comparison row "Strictly sequential"; `:392` "as an engine it also doesn't parallelize and doesn't spawn workers"; `:424` "an akm workflow is inert without an agent to execute its"; Part F at `:520-532` still lists fan-out and watch as FUTURE work. All invalidated by the shipped engine: src/workflows/exec/{native-executor,scheduler,watch,worktree,unit-writer}.ts, `workflow_run_units` + migration 004. The subcommand list at :243-245 covers only start/next/complete — missing run/brief/report/watch/create. And the file never mentions the `experimental.workflowEngine` gate: verified live, `akm workflow run foo` returns WORKFLOW_ENGINE_NOT_ENABLED.

Files: `docs/guides/claude-code-vs-akm-workflows.md`

### Q-01 — mv ruling: scaffolded facts/conventions/organization.md never rewritten; ships into every user stash **[D-49 class]**

Ruling required rewriting the scaffolded fact to teach mv. organization.md:109-110 still says 'A rename is delete plus create. There is no command that preserves an asset's identity or learned state across one'; only shell `mv` at :114; `akm mv` absent. git log 28e4b55..HEAD on the file is EMPTY. Verified live: `init --dest $SB/stash` copies it verbatim. D-49: 4d82df1 fixed the SIBLING src/assets/stash-skeleton/README.md and its body says 'The worst of these ships into every user stash', so the skeleton reads as handled. cli-hints-full.md:211-217 WAS fixed to teach mv, so the two shipped agent surfaces now contradict each other.

Files: `src/assets/stash-skeleton/facts/conventions/organization.md`, `src/assets/hints/cli-hints-full.md`

### Q-06 — Fragment refs: 'show-only in 0.9.0' scope sentence never added to ref.md / D1

Ruling was doc-only: add one scope sentence to ref.md #Fragments and D1's Status line. ref.md:109-131 reads as if fragments resolve generally; 0.9.0-decisions.md:24 says 'Status: SHIPPED' with no show-only scope. Code unchanged: parseRefInput still throws UsageError 'Export fragment "#..." is not accepted here' (resolve-ref.ts:311-314); only show bypasses. Neither file was touched by the program.

Files: `docs/architecture/specs/ref.md`, `docs/architecture/specs/0.9.0-decisions.md`, `src/core/asset/resolve-ref.ts`

### Q-09 — list --kind taxonomy: guide swept, authoritative cli.md left teaching local|managed|remote **[D-49 class]**

Ruling: keep code taxonomy, fix docs. sources-registries.md WAS swept (zero hits). cli.md `### list` still opens 'Show all sources — local directories, managed packages, and remote providers', gives four `--kind local|managed|remote|local,remote` examples and a flag row listing those values. Code: VALID_SOURCE_KINDS = filesystem|git|npm|website (sources-cli.ts:34). Live: `list --kind local` and `--kind managed` both -> INVALID_FLAG_VALUE. cli.md was edited twice (24f9a81, fadc65e). 24f9a81's body cites '`akm list --kind managed`' as one of four verified P0s that justified DELETING the agent corpus — while the same defect survives in the reference doc that calls itself authoritative.

Files: `docs/reference/cli.md`, `src/commands/sources/sources-cli.ts`

### Q-10 — curate --rerank still advertised by the live setup wizard and cli.md **[D-49 class]**

Ruling: remove the cli.md reference and the setup-wizard prose. Both survive: connection.ts:365 '  • akm curate --rerank   (search reranking)' and :424 '  • akm curate --rerank'; cli.md:150 'akm curate --rerank'. Flag does not exist: zero 'rerank' in search-cli.ts, `curate --help` has 0 hits. D-49 is exact: e34452a edited connection.ts for NEW-2 and removed `akm distill` from :363 and :422 — the lines immediately above the two --rerank lines it left.

Files: `src/setup/steps/connection.ts`, `docs/reference/cli.md`

### Q-11 — cli.md still claims workflow names support forward slashes **[D-49 class]**

The ruling said this is doc-only and named the exact lines ('Only docs/reference/cli.md:574-577 is wrong'). cli.md:615-616 still reads 'Forward slashes are supported for hierarchical names (e.g. `release/ship`)'. Live: `workflow create release/ship` -> INVALID_FLAG_VALUE "Asset --name must be a flat name without '/'. Use --path...". cli.md was edited twice by the program.

Files: `docs/reference/cli.md`, `src/commands/workflow-cli.ts`

### Q-12 — D9/E4 wording alignment (the ruling's remaining half) not done

0.9 runtime half landed. But 0.9.0-decisions.md:387-389 still says 'Decided. The strict 0.9 runtime does not parse the retired confidence-gate flag' two lines below its own 'Status: SHIPPED' at :385 — self-contradicting. 0.9.0-release-surface-review.md:479-481 E4 repeats 'The strict 0.9 runtime does not preserve a dead confidence-gate flag'. Neither spec file was touched by the program.

Files: `docs/architecture/specs/0.9.0-decisions.md`, `docs/architecture/specs/0.9.0-release-surface-review.md`

### Q-13 — Registry stale-fallback windows: no code, docs still promise 7d/24h **[D-49 class]**

Ruled a regression to restore. registry-index-cache-repository.ts:78 still `if (... Date.now()-fetchedAt > maxAgeMs) return undefined`, and registry-cache.ts:109 feeds that same TTL-bounded getter into dbCacheResult, so the stale branch at :139-142 can only fire inside TTL. No staleWindowMs anywhere. registry.md:325 '7-day stale fallback' and :345 'Stale cache fallback (up to 24 hours) on network failure' still stand. All three files WERE touched by the program; no commit body mentions the windows.

Files: `src/storage/repositories/registry-index-cache-repository.ts`, `src/storage/repositories/registry-cache.ts`, `docs/reference/registry.md`

### Q-14 — Tier/doc convention: lint, proposal drain, improve canary still untiered or undocumented **[D-49 class]**

extract got its cli.md section (fadc65e) and backup is moot (R-029). Still open: `akm lint` has a STABILITY bullet at :191 but no `### lint` section in cli.md (46 `###` sections, none matching lint/drain/canary); `akm proposal drain` (registered proposal-cli.ts:519) has no tier row and no section; `akm improve canary` (registered improve-cli.ts:123) has zero occurrences of 'canary' anywhere in cli.md. STABILITY.md was edited twice by the program.

Files: `STABILITY.md`, `docs/reference/cli.md`, `src/commands/proposal/proposal-cli.ts`, `src/commands/improve/improve-cli.ts`

### Q-15 — wikis.md still says raw/ is never indexed

Ruled code-wins/doc-fix. docs/guides/wikis.md:59-60 still: '`raw/`, `schema.md`, `index.md`, and `log.md` are structural — they are never indexed as pages', with no statement that raw/**.md IS indexed as first-class searchable `wiki-source` documents (llm-wiki-adapter.ts:83-84,255,274). File was NOT touched by the program.

Files: `docs/guides/wikis.md`, `src/core/adapter/adapters/llm-wiki-adapter.ts`

### Q-16 — functional-contract-patterns.md §4 never trimmed to the five implemented contracts

Ruled: delete the seven unimplemented entries and rename the §3.9 seam to RunnerSpec/executeRunner. §4 still lists PathResolver (:243), MatchContributor (:247), MetadataContributor (:251), LintContributor (:271), ImproveContributor (:275), IndexPostProcessor (:279), AgentRunner (:283), and §3.9 still states 'harness-specific launch logic lives behind one `AgentRunner`' (:221). File NOT touched by the program.

Files: `docs/architecture/internals/functional-contract-patterns.md`

### Q-19 — resolveSourcesForOrigin still cannot match install refs; doc + error message stay aspirational

Ruled: fix the resolver so `//meta` and the akm add hint work. origin-resolve.ts:19-24 is unchanged — `installations[index]?.id === origin` only, and deriveInstallations ids are slug-legal bundle ids (installations.ts:98-125), never `github:owner/repo`. show.ts:150 still emits 'Run: akm add ${metaRef.origin}'. concepts.md:352 still documents `akm show github:owner/repo//meta`. The register recommended landing this with R-007; 6c3f33e landed R-007 and left this untouched (git log on origin-resolve.ts is empty).

Files: `src/registry/origin-resolve.ts`, `src/commands/read/show.ts`, `docs/guides/concepts.md`

### D-04 — STABILITY proposal-status vocabulary still wrong **[D-49 class]**

STABILITY.md:183-184 still lists 'accepted, pending, proposed, rejected, archived'. Code: `ProposalStatus = pending|accepted|rejected|reverted` (proposal-types.ts:157). `proposed` and `archived` are not statuses; `reverted` (documented at proposal-types.ts:150 as a real status) appears nowhere in STABILITY.md. File touched twice by the program.

Files: `STABILITY.md`, `src/commands/proposal/proposal-types.ts`

### D-06 — Retired llm.* keys: root AGENTS.md fixed, indexing.md and a live indexer warn() left **[D-49 class]**

AGENTS.md:50 was corrected ('akm config set/unset llm... fails with Unknown config key'), verified live. Still wrong: docs/architecture/internals/indexing.md:6 ('When `akm.llm` is configured'), :78 ('`llm.concurrency` in config.json overrides both'), :88 ('llm.timeoutMs') — file NOT touched. Worse, indexer.ts:1857 is a live user-facing warn(): 'LLM enrichment budget exceeded... Increase llm.timeoutMs for a larger budget' — plus comments at :252, :1683, :1704-1705, :1848, :2290. Verified live: `config set llm.timeoutMs 600000` -> 'Unknown config key: llm.timeoutMs'. indexer.ts WAS touched three times (fa6df0c, 930d380, 9e74419).

Files: `src/indexer/indexer.ts`, `docs/architecture/internals/indexing.md`

### D-07 — Error-envelope shape in AGENTS.md still omits `hint` **[D-49 class]**

The exit-code half was fixed — AGENTS.md:40 now lists 0/1/2/4/70/78. The envelope half was not: it still says failures render as `{ok:false, error, code}` while cli/shared.ts:113 emits `{ok:false, error, code, hint}` (confirmed in every live error above). AGENTS.md was touched by 026cfb9. Adjacent, not in the register: the root `akm --help` exit-code banner omits 70, which both AGENTS.md and STABILITY.md:117 document.

Files: `AGENTS.md`, `src/cli/shared.ts`, `src/cli.ts`

### D-08 — Global-flag table still missing the --output row **[D-49 class]**

Register's remaining work was literally 'one table row'. cli.md:16-22 lists --format/--detail/--shape/--quiet/--verbose only. `--output` is a real global flag, shown in live `akm --help` ('Write rendered output to a file instead of stdout (all formats except jsonl)') and present in every bash-completion flag list. cli.md touched twice.

Files: `docs/reference/cli.md`

### D-09 — init dir list still missing tasks/, sessions/, facts/ **[D-49 class]**

cli.md `### init` still says 'currently `scripts/`, `skills/`, `commands/`, `agents/`, `knowledge/`, `workflows/`, `memories/`, `env/`, `secrets/`, and `lessons/`' — 10 dirs. Verified live: init creates 13 (adds facts, sessions, tasks). cli.md touched twice; the register even flagged this for re-sweep after Q-18.

Files: `docs/reference/cli.md`

### D-10 — cli.md indexing claim still uses the retired akm.llm key **[D-49 class]**

cli.md:195 'When `akm.llm` is configured and the per-pass gate allows it, metadata...'. `llm` is hard-rejected at config load (config-schema.ts:169 '<key> is retired in 0.9'), confirmed live.

Files: `docs/reference/cli.md`

### D-11 — health --group-by still has no flag-table row **[D-49 class]**

--report and --window-compare are now documented (cli.md:246-247). `--group-by` is a declared arg (cli.ts:312, consumed :341) and appears only in prose at cli.md:40, never in the health flag table. `--windows` appears to no longer exist in code, so that half looks moot.

Files: `docs/reference/cli.md`, `src/cli.ts`

### D-12 — cli.md still self-contradicts on --shape summary for search **[D-49 class]**

cli.md:71 correctly says summary is 'Valid only on `akm show`. Every other command rejects `--shape summary`'. cli.md:402 and :432 still say 'For `search`, `summary` currently behaves the same as the default `brief` envelope' / 'currently identical to `brief`'. Verified live: `search foo --shape summary` -> INVALID_SHAPE_VALUE. cli.md touched twice.

Files: `docs/reference/cli.md`

### D-13 — search --type list still incomplete; shape-source pointer still wrong **[D-49 class]**

cli.md search flag table lists skill|command|agent|knowledge|workflow|memory|script|env|secret|lesson|any — still missing `task`, `session`, `fact` (and now `instruction`, all in KNOWN_TYPES). Shape-source pointer still cites `src/output/shapes.ts` for functions living in src/output/shapes/helpers.ts. --belief has a table row; --no-project-context and --include-sessions remain undocumented.

Files: `docs/reference/cli.md`, `src/core/recognition-util.ts`

### D-14 — show: path/editable still documented as --detail full additions **[D-49 class]**

cli.md show section still: '`--detail full` adds verbose metadata such as `schemaVersion`, `path`, and `editable`'. helpers.ts:492-495 carries the literal comment 'path and editable are always projected so JSON consumers can locate...' and projects both unconditionally; only schemaVersion/editHint are full-only.

Files: `docs/reference/cli.md`, `src/output/shapes/helpers.ts`

### D-15 — workflow: 5 of 14 subcommands still undocumented; hierarchical-name claim intact **[D-49 class]**

cli.md workflow subcommand table lists 9 (template, create, validate, start, next, complete, status, list, resume). Live `workflow --help` USAGE: start|next|complete|status|list|create|template|resume|abandon|validate|run|brief|report|watch — run/brief/report/watch/abandon all missing from the doc. Slash-name claim survives (see Q-11). This matters more now: the four missing engine verbs are exactly the ones 05537cf gated behind experimental.workflowEngine, and cli.md says nothing about the gate or exit 78.

Files: `docs/reference/cli.md`, `src/commands/workflow-cli.ts`

### D-17 — list --kind: all four documented examples still exit non-zero **[D-49 class]**

See Q-09 — same defect, cli.md `### list`. Also `remove --yes/-y` remains undocumented.

Files: `docs/reference/cli.md`

### D-18 — feedback/history/log flags still entirely undocumented **[D-49 class]**

Zero occurrences in cli.md of --failure-mode, --include-proposals, --accept-rate-by-source, --include-tags, --exclude-tags. All five are declared: feedback-cli.ts:238, sources-cli.ts:253 and :260, observability-cli.ts:68 and :72. cli.md touched twice; 251d137 even edited a --failure-mode claim in improvement-loop.md without adding the cli.md rows.

Files: `docs/reference/cli.md`, `src/commands/feedback-cli.ts`, `src/commands/sources/sources-cli.ts`, `src/commands/observability-cli.ts`

### D-19 — registry 'four subcommands' claim and undocumented remove --yes **[D-49 class]**

cli.md:1433 still 'The `registry` command has four subcommands' while five ship (live USAGE: list|add|remove|search|build-index) and the doc itself documents five. `registry remove --yes/-y` exists (registry-cli.ts:85 'Skip confirmation prompt') and has 0 hits in the `#### registry remove` block.

Files: `docs/reference/cli.md`, `src/commands/registry-cli.ts`

### D-20 — config validate/show/enable/disable and --silent entirely undocumented **[D-49 class]**

ZERO occurrences in cli.md of 'config validate', 'config show', 'config enable', 'config disable', or '--silent'. All four are registered: config-cli.ts:172 show, :231 validate, :241 enable, :251 disable. `validate` is load-bearing (startup bypass allowlist, cli.ts) and enable/disable are referenced by registry.md. cli.md touched twice.

Files: `docs/reference/cli.md`, `src/commands/config-cli.ts`

### D-21 — env subcommand table still omits set/unset **[D-49 class]**

cli.md env subcommand table lists list|run|create|path|export|remove. `set` and `unset` are registered and appear in the examples 30 lines above (`akm env set env/prod DATABASE_URL`, `akm env unset env/prod DEBUG OLD_FLAG`) but never in the table. create's --sensitive/--path/--target still appear only in prose/examples, never in a flag table.

Files: `docs/reference/cli.md`, `src/commands/env/env-cli.ts`

### D-22 — improve: archiveRetentionDays wrong scope+default; canary and 4 flags undocumented **[D-49 class]**

cli.md:2031 still 'proposal older than `improve.archiveRetentionDays` (default 30)'. Truth: top-level key (config-schema.ts:151) with default 90 (proposal/repository.ts:1053) — and configuration.md:211 now says 90, so the two reference docs contradict each other. Undocumented: --skip-if-locked (improve-cli.ts:165), --sync/--no-sync, --push; and 'canary' has ZERO occurrences in cli.md though `improve canary [--refresh]` is registered (improve-cli.ts:123,139). D-49 is sharp: fadc65e added the --no-push row (4 hits) to that very flag table and left the rest.

Files: `docs/reference/cli.md`, `src/commands/improve/improve-cli.ts`, `src/core/config/config-schema.ts`

### D-23 — proposal drain undocumented while cli.md still asserts a six-verb grammar **[D-49 class]**

cli.md `### proposal`: 'The canonical grammar is `akm proposal <verb>`: list, show, diff, accept, reject, revert' and :2086 'All six verbs...'. `drain` is registered at proposal-cli.ts:519 with its own flags (--profile retirement handled at :388). Bulk --max-diff-lines/--older-than/--dry-run also undocumented.

Files: `docs/reference/cli.md`, `src/commands/proposal/proposal-cli.ts`

### D-24 — cli.md still documents tasks_invoked/tasks_completed events that do not exist **[D-49 class]**

cli.md tasks section: '`tasks_invoked` and `tasks_completed` events land in `state.db` so runs are auditable via `akm log`'. grep -rn 'tasks_invoked|tasks_completed' src/ returns NOTHING. Task runs write the state.db `task_history` table (runner.ts:24, task-history-repository.ts:6), surfaced by `akm tasks history`. `tasks add --disabled/--force` and init's CI skip also undocumented (--rebind is now covered).

Files: `docs/reference/cli.md`, `src/tasks/runner.ts`

### D-25 — akm lint still has no cli.md section **[D-49 class]**

fadc65e added `### extract` (cli.md:1952) and backup is moot post-R-029. `akm lint` is a registered top-level command (live `akm --help` USAGE includes it) and has no `### lint` heading among cli.md's 46 sections — only three incidental prose mentions at :931, :956, :1013.

Files: `docs/reference/cli.md`

### D-26 — Four dead configuration.md anchors still linked from cli.md **[D-49 class]**

cli.md:344 -> configuration.md#graph-boost-search-tuning; :1091 and :1206 -> #defaultwritetarget; :1168 -> #memory-scope. configuration.md's headings are: Version 0.9, Engines, Strategies, Indexing, Semantic search, Search tuning, Feedback, Bundles and write target, Output defaults, Setup-derived recommendations, Experimental opt-ins, Managing Config, Environment, Retired Configuration — none generate those anchors. Directly contradicts 24f9a81's body claim 'Docs: dead links... fixed'. Both files touched.

Files: `docs/reference/cli.md`, `docs/reference/configuration.md`

### D-27 — agent --cwd and propose --path still undocumented **[D-49 class]**

'--cwd' has 0 occurrences in cli.md; the `### propose` section has 0 occurrences of '--path'.

Files: `docs/reference/cli.md`

### D-28 — configuration.md: 10 of 11 keys now documented; `registries` still absent **[D-49 class]**

bundles, defaultBundle, defaultWriteTarget, search, feedback, archiveRetentionDays, output, semanticSearchMode, embedding, setup all have real coverage now. `registries` has ZERO occurrences in configuration.md yet is a valid top-level key (confirmed live in the INVALID_FLAG_VALUE hint's key list) and is what `akm registry add` writes. File touched by the program.

Files: `docs/reference/configuration.md`

### D-29 — Environment table still missing seven variables **[D-49 class]**

configuration.md:284-291 lists 6 vars (AKM_CONFIG_DIR, AKM_ENGINE_<NAME>_API_KEY, AKM_LLM_API_KEY, AKM_EMBED_API_KEY, AKM_STASH_DIR, AKM_SQLITE_JOURNAL_MODE). All of AKM_DATA_DIR, AKM_CACHE_DIR, AKM_STATE_DIR, AKM_VERBOSE, AKM_DEBUG, AKM_DISABLE_PROJECT_CONTEXT are real in src (storage/locations.ts, core/paths.ts, core/errors.ts, cli.ts, db-search.ts); XDG_* is mentioned only once in passing at :3. File touched by the program.

Files: `docs/reference/configuration.md`

### D-31 — registry.md still teaches retired config.installed and sources[] **[D-49 class]**

registry.md:220 '`config.installed` with its id, source, ref, resolved version'; :316 'installing a stash creates a regular `sources[]`'. Both shapes are hard-rejected at config load (config-schema.ts:194 '<key> is the retired pre-cutover source shape; run `akm-migrate apply`...'). registry.md WAS touched by the program.

Files: `docs/reference/registry.md`, `src/core/config/config-schema.ts`

### D-33 — build-index: 'scans the current directory' false, and help still says v2 while emitting v3 **[D-49 class]**

registry.md:393 'This scans the current directory for asset type directories and produces a v3 index'. buildRegistryIndex (build-index.ts:95-112) does no such scan — it fans out to loadManualEntries + scanNpm + scanGithub. registry-cli.ts:144 command meta still 'Build a v2 registry index from discovery and manual entries' while build-index.ts:112 emits `version: 3`; verified live in `akm registry --help`. registry-cli.ts was touched by 6c3f33e (R-008, R-066 #5).

Files: `docs/reference/registry.md`, `src/commands/registry-cli.ts`, `src/registry/build-index.ts`

### D-34 — registry.md stale-window and cache-layout claims **[D-49 class]**

Same as Q-13 for the windows (registry.md:325, :345). Cache layout shape not re-verified this pass.

Files: `docs/reference/registry.md`

### D-36 — workflow_step_* still attributed to `workflow next` (2 of 3 halves fixed) **[D-49 class]**

distill (:156) and lint --repair (:160) were corrected with explicit 'there is no such command/flag' annotations. But :178-179 still attribute workflow_step_completed and workflow_step_updated to '`akm workflow next`'. Emitter is completeWorkflowStep (workflows/runtime/runs.ts:569, appendEvent at :714), whose only caller is workflow-cli.ts:121 — the `complete` verb. File touched by the program; the commit fixed the neighbours and not this row.

Files: `docs/reference/data-and-telemetry.md`, `src/workflows/runtime/runs.ts`, `src/commands/workflow-cli.ts`

### D-38 — `akm log list --limit 20` still documented — and the flag is silently ignored **[D-49 class]**

data-and-telemetry.md:267 still `akm log list --type search --limit 20`. eventsListCommand (observability-cli.ts:59-...) declares since/type/ref/exclude-tags/include-tags — no `limit`, and no args.limit read anywhere in the file. Verified live: the command exits 0 and returns the unfiltered set, so the documented flag is a SILENT wrong answer, not an error — the same class as B-5 that 4d82df1 called the worst kind of defect. File touched by the program.

Files: `docs/reference/data-and-telemetry.md`, `src/commands/observability-cli.ts`

### D-39 — data-directory table still lists `wikis` as a stash dir **[D-49 class]**

stashDir (:72) and the 0.8.0 reference (:57) are now correct/historical. But :69 still enumerates stash contents as '...env files, secrets, wikis, lessons, facts'. No `wiki` placement spec exists and `akm init` creates no wikis/ (verified live: 13 dirs, none named wikis). File touched by the program.

Files: `docs/reference/data-and-telemetry.md`

### D-40 — akm-eval.md: runner-type count still 'three'; case count changed from 8 to five, still wrong (12 ship) **[D-49 class]**

The full 28b5..HEAD diff of this file is TWO hunks, both dead-file citations (src/core/proposals.ts -> history.ts; src/proactive-verdict.ts -> scripts/akm-eval/src/proactive-verdict.ts). Untouched: :43 'Three runner types ship in Phases 1 + 2' vs nine EvalCaseType members (types.ts:7-16) including reflect-quality and planner-waste, both of which ship cases; :179-181 'The improve-smoke suite ships five retrieval + three proposal-quality cases' while `ls scripts/akm-eval/cases/improve-smoke/` = 12 files (5 retrieval, 3 proposal, 2 planner-waste, 2 reflect). The sweep moved the number from 8 to 8-as-five+three and still omits the two case types entirely.

Files: `docs/reference/akm-eval.md`, `scripts/akm-eval/src/types.ts`, `scripts/akm-eval/cases/improve-smoke`

### D-44 — knowledge-management: 'akm does not edit entries' still contradicted by env set/unset **[D-49 class]**

The --name ops/prod-secrets half is fixed (:16 now `--path ops --name prod-secrets`) and secrets are correctly under secrets/. Still there at :105: '# akm does not edit entries — edit the file with your own editor'. `env set`/`env unset` do exactly that (cli.md: 'env set/env unset do a minimal, comment-preserving edit'), and the same fenced block runs `akm secret set` 12 lines later. File touched by 24f9a81.

Files: `docs/guides/knowledge-management.md`

### D-45 — improvement-loop: 'there is no auto-accept threshold' still false **[D-49 class]**

improvement-loop.md:84-85 'Generated proposals always queue for review; there is no auto-accept threshold.' src/assets/improve-strategies/proactive-maintenance.json ships triage `applyMode: "promote"`, `maxAcceptsPerRun: 100`, and `proposal drain --apply/--promote` is a second promote surface. The doc's other two halves (--reason requirement at :27-31, --since parser at :68-72) WERE fixed — by 24f9a81 and then 251d137 — and both commits left the headline claim.

Files: `docs/guides/improvement-loop.md`, `src/assets/improve-strategies/proactive-maintenance.json`

### D-49 — claude-code-vs-akm-workflows.md Parts B–E still describe the pre-engine world **[D-49 class]**

THE canonical instance, confirmed exactly as WAVE2-CARRYOVER predicted. The file's entire 28b5..HEAD diff is ONE hunk: prose moved out of a code fence at :240-250. Surviving: :238 '**akm does not execute workflow steps.**'; :164 'An akm workflow **is a document**, not a program'; :236 heading 'B.4 Execution model: akm tracks; the agent executes'; :424 'an akm workflow is inert without an agent to execute its...'; :346 'Fixed linear step sequence'; :522 fan-out as 'a natural next step'; :532 check-in/watch as future. Reality: src/workflows/exec/ ships native-executor.ts, scheduler.ts, worktree.ts, watch.ts, run-workflow.ts; workflow_run_units exists (workflow-runs-repository.ts:61); live `workflow --help` shows 14 verbs vs the guide's B.9 list of 3. The file never mentions experimental.workflowEngine or the exit-78 refusal (verified live).

Files: `docs/guides/claude-code-vs-akm-workflows.md`, `src/workflows/exec/workflow-engine-gate.ts`

### D-50 — README.npm.md still advertises skills.sh community search that ships disabled

.github/README.npm.md:84 'One command to search everything -- Local stash, registries, and community skills from skills.sh in a single query.' src/core/config/config.ts:111 ships `{ url: "https://skills.sh", name: "skills.sh", provider: "skills-sh", enabled: false }`. File was NOT touched by the program; PHASE3-COMMENTS-TRIAGE cleared it only for command-existence claims. The asset-type-table half appears moot — the file no longer carries that table.

Files: `.github/README.npm.md`, `src/core/config/config.ts`

### D-51 — Dead symbols mostly swept; akmManifest() survives in search.md

Fixed: architecture.md no longer cites the three re-export shims; storage-locations.md no longer cites TYPE_DIRS; improve-workflow.md no longer cites parseAssetRef; fact-asset-type.md's table had 5 rows corrected (diff verified). Still open: docs/architecture/internals/search.md:219 'The manifest API (`akmManifest()`) returns a compact listing of all assets' — zero akmManifest in src/, and search.md was NOT touched. Remaining TYPE_DIRS/parseAssetRef hits are all in chartered-historical spec files.

Files: `docs/architecture/internals/search.md`

### D-52 — architecture.md still documents a four-method RegistryProvider **[D-49 class]**

architecture.md:292-298 declares `searchKits(q)`, `searchAssets?(q)`, `getKit(id)`, `canHandle(ref)`. The real contract (registry/providers/types.ts:35-45) is `readonly type: string` plus a single `search(options)`. architecture.md WAS touched by 24f9a81.

Files: `docs/architecture/architecture.md`, `src/registry/providers/types.ts`

### D-53 — storage-locations: one proposal reference fixed, three tables still document on-disk proposals and graph.json **[D-49 class]**

:265 was corrected to 'Replaces per-uuid JSON directories under $STASH/.akm/proposals/'. Untouched: :486-487 still document `$STASH/.akm/proposals/<uuid>/proposal.json` and `.../archive/<uuid>/proposal.json` as live paths with retention, and :649-651 list `$STASH/.akm/graph.json` plus both proposal paths as durable artifacts. Zero `graph.json` references exist in src/. Also :430 still attributes backupExistingConfig() to `src/core/paths.ts`; it is exported from src/core/config/config-io.ts:111. File touched by the program.

Files: `docs/architecture/internals/storage-locations.md`, `src/core/config/config-io.ts`

### D-54 — indexing.md documents 7 tables; 19 are created

indexing.md table inventory lists entries, entries_fts, embeddings, entries_vec, utility_scores, index_meta, workflow_documents. index-schema.ts has 19 CREATE TABLE statements. File was NOT touched by the program.

Files: `docs/architecture/internals/indexing.md`, `src/storage/repositories/index-schema.ts`

### D-55 — architecture.md: pushOnCommit 'deprecated' (it is rejected), renderers.ts 'replaced' (it exists), static-index 'v2' (reads v2+v3) **[D-49 class]**

The `wiki` type row was removed. Still wrong: :235 'The old per-asset commit/push path (`options.pushOnCommit`) is deprecated and no longer commits per asset' — config load HARD-REJECTS it (sources-bundles.ts:31-35 'options.pushOnCommit is not supported'); :490 '(#490; replaces the old `renderers.ts`)' while src/output/renderers.ts still exists at 600 lines and self-registers; :306 '`static-index` | Reads the v2 JSON index schema' while static-index.ts:125-127 accepts version 2 AND 3. File touched by 24f9a81.

Files: `docs/architecture/architecture.md`, `src/core/config/schema/sources-bundles.ts`, `src/output/renderers.ts`, `src/registry/providers/static-index.ts`

### D-56 — bundle-adapter-spec still cites `akm bundle rename` as first-class; deferred facets unmarked

akm-0.9.0-bundle-adapter-spec.md:101 '`akm bundle rename` is a first-class rekey transaction (normative §11.5)' — the `bundle` command group was removed by D5. The optional facet methods at :169, :173, :177 still carry no deferred-status marker despite bundle-adapter.ts's 'DEFERRED ... FLAGGED for maintainer'. File NOT touched.

Files: `docs/architecture/specs/akm-0.9.0-bundle-adapter-spec.md`

### D-57 — fresh-host-rebuild-runbook still teaches the retired stashDir key

fresh-host-rebuild-runbook.md:26 'Clone your stash git remote to the `stashDir`'. Key is hard-rejected. File NOT touched by the program.

Files: `docs/architecture/internals/fresh-host-rebuild-runbook.md`

### D-62 — Migration verification snippet still pipes a nonexistent .stashDir field

docs/migration/v0.8-to-v0.9.md:418 `ls -la "$(akm info --format=json | jq -r .stashDir)/vaults/.migrated"`. Verified live: InfoResponse has schemaVersion/version/assetTypes/searchModes/semanticSearch/... and no stashDir, so jq yields 'null' and the ls resolves to `null/vaults/.migrated`. File deliberately excluded from the sweep as chartered-historical — but this is an executable verification step a migrating user runs, so the exclusion decision should be revisited.

Files: `docs/migration/v0.8-to-v0.9.md`

### NEW-A — WAVE2-CARRYOVER item never routed: RESPONSE_CONTRACT_JSON still teaches "<type>:<name>" to agents **[D-49 class]**

WAVE2-CARRYOVER's first entry: prompts.ts:99 RESPONSE_CONTRACT_JSON teaches the retired colon grammar in an agent-facing response contract, 'Not yet fixed — assign in wave 2.' It was never assigned. prompts.ts:100 still reads: 'Shape: {"ref": "<type>:<name>", "content": ..., "frontmatter": {...}, "confidence": ...}'. Consumed at reflect.ts:909 (`parsed.ref`) and parsed by parseRefInput at reflect.ts:1321, which rejects colon refs. Two MORE sites in the same file that no register or carryover lists: prompts.ts:290 and :399 both instruct the model to return '`knowledge:skills/<skill>/references/<topic>`'. prompts.ts WAS touched by 290beb9, which fixed only the env line at :78 — exactly the scoping the carryover described.

Files: `src/integrations/agent/prompts.ts`, `src/commands/improve/reflect.ts`

### NEW-B — The zero-tolerance grammar gate still cannot see src/**/*.ts — the blind spot that produced E-1 and NEW-A

scripts/lint-shipped-assets.ts:58 SCAN_ROOTS = ["src/assets", "scripts/akm-asset", "scripts/akm-eval/cases", "scripts/akm-eval/example-stash"]. 24f9a81's body claims 'The gate's scan roots are extended so it cannot recur' — true only for the eval example stash. e6ea2ae's own body states the gate 'could not catch this: its scan roots cover src/assets and eval cases, not src/**/*.ts' and did not widen it. So every colon-grammar emitter inside TypeScript (NEW-A, brief.ts) remains structurally invisible to CI.

Files: `scripts/lint-shipped-assets.ts`

### NEW-C — Part 4 landmine: context.ts still asserts the citty premise the register disproved **[D-49 class]**

Part 4 recorded '[verified live]: citty auto-camelizes... this disproves the comment at src/output/context.ts:87-93 ("citty does not auto-camelise hyphenated arg keys") — the codebase's quoted-kebab arg convention rests on a false premise, worth a note where that comment lives.' context.ts:91 still says 'citty does not auto-camelise hyphenated arg keys'. context.ts WAS edited by 24f9a81 (for the A-22 runMain reference, per PHASE3-COMMENTS-TRIAGE) — the agent fixed one false claim in the file and left the one the register had already disproved.

Files: `src/output/context.ts`

### NEW-D — Part 4 landmine: three adapter docblocks still claim the adapters barrel is test-only; production imports it **[D-49 class]**

Part 4 named llm-wiki-adapter.ts:63-64 as the 'stale code comment that misled the audit itself'. Still there: llm-wiki-adapter.ts:63 'Imported ONLY by the test-only `adapters/index.ts` barrel (nothing in `src/` imports that)'. src/core/adapter/registry.ts:33 does `import { BUILTIN_ADAPTERS } from "./adapters"` and re-exports at :36 — production. Two further instances the register never listed carry the same false claim: akm-adapter.ts:74 and tool-dir-shared.ts:45. This is the identical class 24f9a81 fixed across 14 harness docblocks ('asserted they were NOT registered anywhere while the registry wires every one of them'); llm-wiki-adapter.ts was in that very commit.

Files: `src/core/adapter/adapters/llm-wiki-adapter.ts`, `src/core/adapter/adapters/akm-adapter.ts`, `src/core/adapter/adapters/tool-dir-shared.ts`, `src/core/adapter/registry.ts`

### NEW-F — Register self-citations point at files this program deleted

WAVE2-CARRYOVER flagged this. 0.9.0-docs-code-drift-register.md:168, :271, :415, :621, :631 cite docs/agents/AGENTS.full.md and docs/agents/AGENTS.md with line numbers; docs/agents/ now contains only README.md, agent-install.md, curate-workmap.md. The register itself was not touched by the program, so every status annotation in it is pre-program and must be re-verified rather than trusted.

Files: `docs/architecture/specs/0.9.0-docs-code-drift-register.md`

### Part5-2 — Sweep the agent-copy-paste surfaces **[D-49 class]**

Largely done for hints, show footer, error messages, help examples. Not done for three shipped/agent-facing surfaces: the scaffolded conventions fact (Q-01), RESPONSE_CONTRACT_JSON and the two knowledge: prompt examples (NEW-A), and the setup wizard's curate --rerank lines (Q-10). Also brief.ts:289 and param-secrets.ts:17,78 describe env bindings as '`env:` refs' in warning text an agent reads, a spelling now rejected loudly.

Files: `src/integrations/agent/prompts.ts`, `src/setup/steps/connection.ts`, `src/workflows/exec/brief.ts`, `src/assets/stash-skeleton/facts/conventions/organization.md`

### Part5-3 — Re-tier STABILITY.md **[D-49 class]**

Workflow exec/driver family tiered; extract documented. Still unassigned: proposal drain and improve canary have no tier and no cli.md section; lint has a tier bullet but no section. And STABILITY.md:73-76 carries a technically false mv claim (D-03) plus a wrong proposal-status vocabulary (D-04).

Files: `STABILITY.md`, `docs/reference/cli.md`

### Part5-4 — Mechanical doc sweep + CI check that executes fenced akm examples

The sweep ran but left the residue above. The proposed CI check does not exist — no lint-* script under scripts/ executes fenced `akm ...` examples from docs/, and ci.yml has no such job. Every one of the failures I reproduced live this pass (list --kind local, workflow create release/ship, search --shape summary, log list --limit, env run env:prod, info | jq .stashDir) is one that check would have caught.

Files: `scripts`, `` .github/workflows/ci.yml``

### A1 — `akm search --no-project-context` is still a total no-op, and the documented env-var fallback still does not exist **[D-49 class]**

UNFIXED IN EVERY PART. `search-cli.ts:65` still declares the arg literally as `"no-project-context"`. Probed citty directly (`bun` + `parseArgs`): `--no-project-context` yields `{"no-project-context":false,"project-context":false}` — byte-identical to omitting the flag. So `getHyphenatedBoolean(args,"no-project-context")` (search-cli.ts:96) returns false either way and `disableProjectContext`/`disableScopedUtility` are never set. `AKM_DISABLE_PROJECT_CONTEXT` is read nowhere in src/ (only comments at db-search.ts:449, ranking-contributors.ts:490) yet is still ADVERTISED to users in the help string at search-cli.ts:68. The test the item said to replace is still the env-mutation tautology at tests/integration/commands/search-cli-envelope.test.ts:86 ("does not mutate process env"). D-49 CLASS: commit 24f9a81 (Phase 3 sweep) edited this exact file 14 lines above, stripping "(v1 spec §4.2)" from :54, and e6ea2ae edited it again — the file reads as handled in two commits while A1 and C9's `AKM_DISABLE_PROJECT_CONTEXT` deletion sat untouched in the same arg block.

Files: `src/commands/read/search-cli.ts`, `src/output/context.ts`, `src/indexer/search/db-search.ts`, `tests/integration/commands/search-cli-envelope.test.ts`

### A2 — The `blocked` semantic warning is still undifferentiated — 'not configured' is still reported as a fault

Unfixed. `src/indexer/search/db-search.ts:224-227` is still a single unsplit branch: `if (config.semanticSearchMode === "auto" && semanticStatus === "blocked") warnings.push("Semantic search is currently blocked. Using keyword search until the semantic backend is healthy again.")`. The `pending` branch immediately above it (:205-217) DOES carry the `!config.embedding?.endpoint || !config.embedding?.model` predicate the item told the `blocked` branch to reuse, so the fix is a copy of code sitting eight lines away and was never made. The status ledger's `reason`/`message` are still discarded (`rawStatus` is read at :198 and only its `providerFingerprint` is consulted). Because owner ruling 9 also never landed (see X-RULING-9), `semanticSearchMode` still defaults to `auto`, so the out-of-box fault-shaped warning A2 describes is reachable exactly as filed.

Files: `src/indexer/search/db-search.ts`

### B1 — D7 is recorded 'SHIPPED' but the generic TEXT fallback — the largest of the three gaps (37 commands) — was never implemented

COMMIT/DECISION CLAIM NOT SUPPORTED BY THE TREE. `0.9.0-decisions.md:240-242` states 'D7 — one output contract. **Status: SHIPPED.**' and 'All six `--format` values work on every command', naming '37 commands silently emitted JSON under `--format text`' as one of the three failure modes it closes. The md and html halves DID land (`src/cli/shared.ts:271-282`, `renderGenericMarkdown`/`renderGenericHtml`), but the text branch at `src/cli/shared.ts:266-267` is untouched: `const plain = formatPlain(...); deliverRendered(plain ?? JSON.stringify(shaped, null, 2), ...)`. B1's stated action was to 'route formatPlain's null branch through the generic md renderer instead of pretty JSON — closes the 37-command text gap in one move.' RAN IT: `bun src/cli.ts lint --format text` emits `{\n  "ok": true,\n  "fixed": [],` (pretty JSON) while `lint --format md` correctly emits `# lint`. `lint` is not in the exempt set. The md branch's own comment at :272-275 asserts 'No command emits JSON under `--format md` any more' — true, but the sibling text branch two cases above still does, for every command lacking a registered formatter. The guard test cannot see it: `tests/integration/output-formats-universal.test.ts:39-48` iterates a HARDCODED 8-command table (all of which have text formatters), and its per-format assertions at :83-95 check `md` must not start with `{` and `html` must contain `<html` but assert NOTHING for `text`. Its docstring at :15-16 claims 'a new command inherits the coverage' — false; the table is not derived from the command registry.

Files: `src/cli/shared.ts`, `src/output/text.ts`, `tests/integration/output-formats-universal.test.ts`, `docs/architecture/specs/0.9.0-decisions.md`

### B3 — Raw-stdout bypasses survive: `config path`, `config validate`, `env path`, `secret path` still ignore `--format`

OPEN. RAN in sandbox: `akm config path --format json` prints the bare path string `/…/config/akm/config.json`, not a `{path}` envelope — `src/commands/config-cli.ts:162` is an unconditional `console.log(configPath)` in the non-`--all` branch, with no format check. `akm config validate --format json` prints the plain sentence 'No config file at … — nothing to validate.' `src/commands/env/env-cli.ts:189` ends the `env path` handler with `process.stdout.write(`${absPath}\n`)` — again unconditional, never reaching `output()`, so `akm env path <ref> --format json` cannot be piped to `jq`. This is the exact breakage B3 names. The `--all` branch of `config path` DOES route through `output("config", result)` (:160), which makes the omission on the bare branch look deliberate but leaves the documented contract broken.

Files: `src/commands/config-cli.ts`, `src/commands/env/env-cli.ts`

### B4 — `hints` was omitted from FORMAT_EXEMPT_COMMANDS, so it silently ignores `--format` with no warning — falsifying STABILITY.md and cli.md

B4's action names the exempt set as 'completions, env run/secret run/agent child stdio, the interactive setup wizard branch, and document payloads (workflow template, `hints`, help migrate)'. `src/output/format-exempt.ts:28-48` declares every one of those EXCEPT `hints`. RAN: `akm completions bash --format md` correctly emits on stderr `[output] '--format' has no effect on 'akm completions' …` (wired at `src/cli.ts:755-760`), but `akm hints --format html` emits raw Markdown (`# akm CLI — Full Reference`) on stdout with an EMPTY stderr. `observability-cli.ts:323` is a bare `process.stdout.write(loadHints(...))` — a document payload identical in kind to `workflow template`. This makes two shipped claims false: `STABILITY.md:105-107` ('The set is declared in src/output/format-exempt.ts, and passing --format to one of them warns rather than silently doing something else') and `docs/reference/cli.md:53-54` ('Passing --format to one of those warns and is otherwise ignored').

Files: `src/output/format-exempt.ts`, `src/cli.ts`, `STABILITY.md`, `docs/reference/cli.md`

### B5 — Cleanup that falls out: the vestigial `distill` registrations survive and text.ts's fallback comment is now doubly wrong

PARTIAL. Done: `OutputConfigSchema.format` is widened to all six (`src/core/config/schema/output.ts:18`); the `reflect` registrations were already absent at 28e4b55; the local `format:` declarations on search/curate/show survive but were re-purposed as documentation with the correct six-value enum (defensible, not open). Open: the `distill` shape (`src/output/shapes/distill.ts:15`) and text formatter (`src/output/text/distill.ts:10`) are still registered although `akm distill` is not a command (RAN: exit 'Unknown command distill') and there is no `output("distill", …)` call site anywhere in src/. Also `src/output/text.ts:15-16` still documents the null branch as meaning 'no plain rendering available — fall back to YAML' — which is stale in a NEW way: the actual fallback at `src/cli/shared.ts:267` is `JSON.stringify`, not YAML, so the comment misdescribes the very behaviour B1 was supposed to remove.

Files: `src/output/shapes/distill.ts`, `src/output/text/distill.ts`, `src/output/text.ts`

### C2 — conceptId-prefix browsing shipped, but the promised flag-table documentation of named `--source` was never written **[D-49 class]**

CODE DONE, DOC RESIDUAL OPEN. `parseRefPrefixQuery` is replaced — `search-cli.ts:36` documents 'A conceptId-prefix query — "memories/projecta/", "bundle//", "bundle//skills/" — enumerates that subtree', and `tests/integration/search-ref-prefix.test.ts` pins it. But C2's closing clause — 'document named `--source` in the flag table (today it is prose-only)' — is verbatim still true. `docs/reference/cli.md:397` (search) and `:466` (curate) both still read `| --source | stash, registry, both | stash |`; the named-bundle capability appears only in prose at `:384`. Meanwhile the SHIPPED agent corpus at `src/assets/hints/cli-hints-full.md:21` already says '`stash`, `registry`, `both`, or a configured bundle name' — so the agent guidance and the human reference now disagree. D-49 CLASS: commit fadc65e edited docs/reference/cli.md for a different reason (adding the missing `### extract` section at :1952 and the `--no-push` row at :2017), which makes the file read as discharged.

Files: `docs/reference/cli.md`, `src/commands/read/search-cli.ts`, `src/assets/hints/cli-hints-full.md`

### C4 — `akm config enable|disable` — the hardcoded skills.sh alias — is still shipping, still listed Stable

OPEN, untouched. `src/commands/config-cli.ts:241-259` still defines both subcommands: `enable: defineJsonCommand({ meta: { name: "enable", description: "Enable an optional component (skills.sh)" } … })` and the mirroring `disable`. The implementation is exactly the single-vendor hardcode C4 objects to — `toggleSkillsShRegistry` at `:101-120` with `SKILLS_SH_URL`/`SKILLS_SH_NAME`/`SKILLS_SH_PROVIDER` constants and a `normalizeToggleTarget` that throws for anything but the literal string `"skills.sh"` (:128-130). The replacement C4 names (`akm registry add https://skills.sh` / `akm registry remove skills.sh`) exists and works, so this is a pure deletion that was never scheduled. No commit in the program touched config-cli.ts for this.

Files: `src/commands/config-cli.ts`

### C6 — `upgrade --skip-checksum` still ships AND is tab-completable, while STABILITY.md documents the exact inverse and a recovery env var that does not exist **[D-49 class]**

OPEN, AND NOW ACTIVELY MIS-DOCUMENTED — worse than when it was filed. The flag is still declared at `src/commands/sources/sources-cli.ts:119` and consumed at `:140-143` → `self-update.ts:303-331`. RAN `akm completions bash`: line 215 offers `--skip-checksum` in the `akm upgrade` completion word list, which is precisely C6's stated concern ('a tab-completable way to disable integrity verification on a self-updating binary'). Meanwhile `AKM_UPGRADE_SKIP_CHECKSUM` — the replacement C6 proposed — exists NOWHERE in the repository: a full-tree grep (excluding node_modules/.git) returns only three hits, all documentation (`STABILITY.md:189`, `STABILITY.md:353`, and this review at :338). So `STABILITY.md:187-189` asserts, of a security-relevant surface, 'Checksum verification is not optional; the recovery hatch is the `AKM_UPGRADE_SKIP_CHECKSUM` environment variable (Internal), **not a flag**' — inverted in both directions: the env var is inert, and the flag it denies is live and completable. `STABILITY.md:353` repeats it as a Supported env-var table row. D-49 CLASS: fadc65e edited STABILITY.md to discharge phase-1/2 doc debt and did not catch the phantom (the claim itself predates the program, added by 6c369e3).

Files: `src/commands/sources/sources-cli.ts`, `src/commands/sources/self-update.ts`, `STABILITY.md`

### C7 — `akm add --allow-insecure` still authorizes two unrelated risks through one flag — the split was never made

OPEN, and visible as a one-line tell. `src/commands/sources/add-cli.ts:247` declares the single `"allow-insecure"` arg; `:256-257` reads it and then literally aliases it to the second, unrelated authorization: `const allowInsecure = args["allow-insecure"]; const allowDangerousKeys = allowInsecure;`. That one value then gates the plain-HTTP transport check at `:262-265` AND the dangerous-env-key install check at `:135`/`:192`/`:213` (`LD_PRELOAD`, `PATH` — keys that hijack execution via `akm env run`, per `src/commands/lint/env-key-rules.ts:15-26`). Neither `--allow-insecure-transport` nor `--allow-dangerous-env-keys` exists anywhere in src/. A user who accepts an HTTP registry to test a mirror silently also accepts arbitrary env-key execution hijacking.

Files: `src/commands/sources/add-cli.ts`, `src/core/activation-policy.ts`

### C8 — Tracker-ID scrub is 4-of-6 done; the two survivors are in files the sweep edited for their siblings **[D-49 class]**

MOSTLY DONE, TWO TEXTBOOK D-49 SURVIVORS. Fixed: the eight `"F-6: "` prefixes in proposal-cli.ts (zero hits remain), `feedback-cli.ts` '(F-3 / #384)' in help, `workflow-cli.ts:62` '(#485)', `search-cli.ts:54` '(v1 spec §4.2)'. STILL RENDERED IN `--help`: (1) `src/commands/sources/sources-cli.ts:263` — the `--accept-rate-by-source` description still ends '…and include them in the output (F-4 / #385).' That file was edited TWICE by the program for neighbouring defects: 0134501 fixed the colon example at :213 and 4d82df1 fixed `akm accept`/`akm reject` at :243 — 20 lines above the label. C8 names '(F-4 / #385)' explicitly, and so does PHASE3-COMMENTS-TRIAGE P2. (2) `src/commands/workflow-cli.ts:150` — the `--units` description still ends '…step evidence stays deterministic and is unaffected (#22).' Commit 24f9a81 removed '(#485)' from :62 of THIS FILE (confirmed in the diff: `-  description: "Allow a parallel run … (#485)"`) and left '(#22)' 88 lines below, in the same sweep. C8 lists '(#22)' by name. Adjacent: `src/commands/sources/source-clone.ts:29` still carries the retired-grammar JSDoc `npm:@scope/pkg//script:deploy.sh` — the identical example string that 0134501 fixed in sources-cli.ts:213; this file was never touched by the program at all.

Files: `src/commands/sources/sources-cli.ts`, `src/commands/workflow-cli.ts`, `src/commands/sources/source-clone.ts`

### C9 — Env-var hygiene: four of the six 'delete now / replace now' actions were never performed

MOSTLY OPEN. (a) 'Delete now: AKM_DEBUG_LLM (one read, zero setters — fold into AKM_VERBOSE)' — still live at `src/commands/improve/consolidate.ts:1331` (`if (process.env.AKM_DEBUG_LLM)`), still the only read, still no setter. (b) 'Delete now: all mentions of the dead AKM_DISABLE_PROJECT_CONTEXT / AKM_DISABLE_SCOPED_UTILITY' — all present, including the user-visible help string at `search-cli.ts:68` (see A1) plus comments at `db-search.ts:449,469` and `ranking-contributors.ts:490`. (c) 'Replace with DI: AKM_ABLATE_CONTRIBUTORS — thread an option through RankEntriesOptions and delete the env read' — the env read is still there at `src/indexer/search/ranking.ts:224` (`const ablateEnv = process.env.AKM_ABLATE_CONTRIBUTORS`). (d) 'Fix the stale AKM_PROFILE_<NAME>_API_KEY comment (the real pattern is AKM_ENGINE_<NAME>_API_KEY)' — `src/core/config/config.ts:360` still names `AKM_PROFILE_<NAME>_API_KEY`; a full grep shows that spelling exists ONLY in that comment, while the real resolver builds `AKM_ENGINE_${…}_API_KEY` (`src/integrations/agent/engine-resolution.ts:137`, `src/core/config/config-walker.ts:314`) — and STABILITY.md:344 documents the AKM_ENGINE_ spelling, so the code comment contradicts the shipped contract. DONE: the `AKM_TEST_MIGRATION_*` hooks left src/ entirely (zero `AKM_TEST_` hits in src/; they now live under scripts/akm-migrate/), and the AKM_ECHARTS comment at `html-report.ts:24-30` is an accurate ledgered behaviour-change note rather than stale text — leave it.

Files: `src/commands/improve/consolidate.ts`, `src/indexer/search/ranking.ts`, `src/core/config/config.ts`, `src/commands/read/search-cli.ts`

### X-AKM-TEST-CLAIM — STABILITY.md claims `AKM_TEST_*` is 'compiled out of release builds' — no build step does that

CLAIM NOT SUPPORTED BY THE TREE. `STABILITY.md:360-361`: 'Anything matching `AKM_TEST_*` is test-only fault injection, compiled out of release builds.' C9 proposed achieving that via `bun build --define` (the AKM_VERSION mechanism). `.github/workflows/release.yml:60-65` is the only compile step and defines exactly one symbol: `--define "AKM_VERSION='$CANDIDATE_VERSION'"`. There is no `--define` for any AKM_TEST_* symbol, and `package.json`'s `build` script is plain tsc + asset copy. The claim is accidentally true for the `akm` binary (it compiles `src/cli.ts`, and AKM_TEST_* is now absent from src/), but false for the second shipped bin: `package.json:52` maps `akm-migrate` to `dist/akm-migrate`, built by tsc from `scripts/akm-migrate/**`, which still reads `AKM_TEST_MIGRATION_CRASH_AFTER`/`CRASH_GAP`/`FAIL_WORKFLOW_DELETE` (config-migrate.ts:1682,1699; three-db-cutover.ts:761,977). Nothing compiles them out of anything.

Files: `STABILITY.md`, `package.json`, `.github/workflows/release.yml`

### E5 — The same-run promotion gap is still open — no `triage.postPass`, no `akm improve --drain`

OPEN, and the code documents the workaround E5 exists to remove. Neither `triage.postPass` nor a `--drain` flag on `improve` exists anywhere in src/ (the only `drain` is the separate `akm proposal drain` subcommand at `src/commands/proposal/proposal-cli.ts:339`). `src/commands/improve/improve-cli.ts:109` still tells users the replacement is 'Replacement: `akm improve && akm proposal drain --promote --yes`, or a `triage` block with …' — i.e. the shell `&&` chaining E5 says 'the documented replacement doesn't need'. Triage still runs pre-pass only. Note the document's own cut line at :609 puts E5 in 'can follow in 0.9.x', so this is scheduled debt rather than a missed release blocker — but it is open and unexamined.

Files: `src/commands/improve/improve-cli.ts`, `src/commands/proposal/proposal-cli.ts`

### G2 — `--source` still carries two different axes; the `--from stash|registry|both` rename was never made

OPEN, and the collision widened rather than narrowed. G2 asked to rename the mode axis on search/curate to `--from stash|registry|both` and leave `--source` for named-bundle narrowing. No `--from` flag exists anywhere. `search-cli.ts:46` still declares `source: { description: "Search source (stash|registry|both)", default: "stash" }`, and `parseSearchSource` (`src/commands/read/search.ts:451-460`) now overloads the SAME token with both meanings — returning the three mode literals, aliasing `local`→`stash`, and passing any other string through as a named bundle for `akmSearch` to validate. Meanwhile `--source` on `graph` still means bundle name/path. So one flag now spans three interpretations across two commands, which is the ambiguity G2 was filed to remove. C2's landing made this deliberate for search, so a decision is arguably required rather than a straight rename.

Files: `src/commands/read/search.ts`, `src/commands/read/search-cli.ts`, `docs/reference/cli.md`

### G3 — `akm tasks --target` still means 'bundle to operate on' — and the CHANGELOG now advertises it as a feature

OPEN, and entrenched since filing. G3 asked to rename tasks' `--target` to `--bundle` because `--target` means write-destination on write commands. `src/commands/tasks/tasks-cli.ts:31-35` still declares `/** Shared `--target <bundle>` arg wired onto every bundle-resolving subcommand. */ const targetArg = { target: { … description: "Bundle to operate on (defaults to the primary/default bundle)" } }`, spread into add/enable/disable/run/sync/history/doctor (`:44`, `:134`, …). More: the CHANGELOG's Unreleased→Added section (`CHANGELOG.md:11-21`) now PROMOTES the collision as a headline feature — 'Schedule tasks from any configured bundle via `--target <bundle>` (#711)' — and records the token in installed scheduler entries as a persisted provenance record. Renaming it later is now a documented breaking change rather than a free cleanup, which is exactly the 'while breaking is still cheap' window G's heading invokes.

Files: `src/commands/tasks/tasks-cli.ts`, `CHANGELOG.md`

### G4 — `claude-code` is still the canonical harness id in code and help; the `claude` unification was not made

OPEN. G4: 'make the harness id `claude` canonical everywhere; `extract --type` accepts both, docs use `claude`.' Tree: `src/integrations/harnesses/claude/index.ts:50-51` keeps `aliases = ["claude-code"]` but `runtimeId = "claude-code"`, and the session-log provider's `name = "claude-code"` (`session-log.ts:119`, emitted into records at `:94`). Consumers branch on the hyphenated form (`src/commands/improve/extract-watch.ts:107`: `if (best.harnessName === "claude-code")`), and `src/commands/improve/extract.ts:400` carries a comment conceding the split ('the Claude provider is "claude-code", but the canonical harness id is …'). User-facing help still teaches the non-canonical spelling: `src/commands/improve/extract-cli.ts:44` — 'Harness name (claude-code, opencode). Required unless --auto.' Note `STABILITY.md:180-182` DOES list the backend as `claude`, so the shipped stability contract and the shipped help string disagree. G4's second half (fix `extract --auto`'s 'every available harness' description) is moot — that wording no longer appears.

Files: `src/integrations/harnesses/claude/index.ts`, `src/commands/improve/extract-cli.ts`, `src/commands/improve/extract.ts`, `STABILITY.md`

### H3 — Env-var table added — but it contains one phantom entry and one unsupported claim

STRUCTURE DONE, CONTENT DEFECTIVE. The table exists at `STABILITY.md:337-361` with the Supported/Internal split H3 asked for, and correctly uses the real `AKM_ENGINE_<NAME>_API_KEY` spelling (:344) plus a genuinely improved `AKM_NPM_REGISTRY` row reflecting R-035. Two defects inside it: (1) row `:353` lists `AKM_UPGRADE_SKIP_CHECKSUM` as a Supported 'recovery hatch for a broken upgrade checksum' — the variable does not exist in the codebase (see C6); (2) the closing line `:360-361` claims `AKM_TEST_*` is 'compiled out of release builds' — nothing compiles it out (see X-AKM-TEST-CLAIM). Both are false statements in the file that is supposed to be the compatibility contract.

Files: `STABILITY.md`

### D-49 — D-49 itself is still open at HEAD, exactly as WAVE2-CARRYOVER predicted **[D-49 class]**

NOT DONE. Commit 24f9a81 touched this file with a +6/-4 diff — the prose-in-code-fence fix only — leaving every substantive claim. `docs/guides/claude-code-vs-akm-workflows.md:236-238` still headlines section B.4 'Execution model: akm tracks; the agent executes / This is the crux. **akm does not execute workflow steps.** It is a persisted state machine driven by an external agent through a CLI command loop', listing only start/next/complete. `:254-256` still asserts 'Strict sequentiality. There is exactly one `current_step_id`.' Both were invalidated by `workflow run`'s native executor and the fan-out scheduler. Part F (`:441`) carries a 'Superseded by the extension plan' banner but its body still proposes integration ideas 'written for the *current* passive-tracker engine' and calls fan-out future work. Critically, the file mentions `experimental.workflowEngine` and exit 78 NOWHERE (zero grep hits) — so a reader following this guide hits a hard refusal with no warning. `docs/reference/workflows.md` has since gained the gate documentation (:16, :186, :202), so this file is now the lone holdout.

Files: `docs/guides/claude-code-vs-akm-workflows.md`

### X-CLI-MD-WORKFLOW — docs/reference/cli.md's workflow subcommand table omits five shipped verbs and never mentions the engine gate **[D-49 class]**

NEW, not recorded in any register I read. RAN `akm workflow --help`: the real surface is `start|next|complete|status|list|create|template|resume|abandon|validate|run|brief|report|watch` (14 verbs). The table at `docs/reference/cli.md:578-588` documents nine and omits `run`, `brief`, `report`, `watch`, and `abandon` — the five the workflow engine added. `workflowEngine` returns ZERO hits in cli.md, so the CLI reference describes the workflow family with no mention that half of it refuses to run until an experimental key is set. D-49 CLASS: commit fadc65e edited this same file to discharge phase-1/2 doc debt (adding `### extract` at :1952 and the `--no-push` row at :2017), which makes it read as swept. This is the same omission class as D-49 and shares its root cause — the workflow engine landed and the human-facing reference was never reconciled.

Files: `docs/reference/cli.md`

### X-RULING-11 — Owner ruling 11 (R-045 / Q-18 second half, marked 'EXECUTE NOW') has ZERO implementation

UNIMPLEMENTED DESPITE AN EXPLICIT EXECUTE-NOW RULING. AGENT-GUARDRAILS.md ruling 11 requires three concrete artifacts: 'a `PLACEMENT_SPECS` entry with an `instructions/` stash dir, an `instructions` row in `src/indexer/walk/matchers.ts`, and a `placementTypes() ⊆ KNOWN_TYPES` subset assertion', and states it SUPERSEDES guardrail 8's prohibition because the Q-18 collision was resolved by `ownsPresentation`. Tree: `src/core/asset/asset-placement.ts:89-170` declares thirteen specs (skill, command, agent, knowledge, workflow, script, memory, env, secret, lesson, task, session, fact) with NO `instruction` entry — a grep for 'instruction' in that file returns nothing. `src/indexer/walk/matchers.ts` has no `instructions` row. No subset assertion exists in tests/ (only `placementTypes()` iteration in goldens-recognition-placement.test.ts:70 and legacy-layout.test.ts:45). Meanwhile `KNOWN_TYPES` (`src/core/recognition-util.ts:91-107`) DOES include `"instruction"` — so the two tables are out of sync in exactly the way the subset assertion was meant to catch. PHASE3-COMMENTS-TRIAGE's KEEP list even anticipated this ('cli-hints-full.md:22 lists instruction — becomes unambiguously right once R-045 lands'); it did not land. Owned by 0.9.0-public-api-issue-backlog.md:462, cross-referenced by this document's A3 and H2.

Files: `src/core/asset/asset-placement.ts`, `src/indexer/walk/matchers.ts`, `src/core/recognition-util.ts`

### X-RULING-12 — Owner ruling 12 (take the breaking changes now) is roughly one-fifth implemented, and the four unlanded items have no CHANGELOG entries

PARTIAL. Ruling 12 lists five breaking changes to land in 0.9.x, each with a mandatory CHANGELOG migration note ('that is the condition attached to the exception, not optional'). Landed: unknown-command exit 1→2 (96ff2fe), with its note at `CHANGELOG.md:81-84`. NOT landed: (a) `installedKitCount` still in the health/index envelope at `src/sources/types.ts:205,287,322`; (b) `graphPath` still in the graph envelopes at `src/commands/graph/graph.ts:32,45,55,65,75,88,99,115` and emitted at `:170,230,259`; (c) the persisted `save` event type is still spelled `"save"` at `src/core/events.ts:56`; (d) the `update`/`upgrade`/`sync` verb renames — all three verbs are still present under their old names in `akm --help`. Grepping CHANGELOG.md for `installedKitCount` and `graphPath` returns zero hits, so no migration notes were written either. PHASE2-TRIAGE anticipated the work ('Guardrail-12's `graphPath` envelope removal WILL break tests/integration/commands/graph.test.ts:212'; '`tests/completions.test.ts` lists the update/upgrade/sync verbs — coordinate with the verb-rename work'), which confirms it was scoped and then dropped rather than ruled out.

Files: `src/sources/types.ts`, `src/commands/graph/graph.ts`, `src/core/events.ts`, `CHANGELOG.md`


## Unclear — needs a run to settle (2)

### R-064 #3 — `acceptedActions` still documented as permanently 0 in the collapse-detector envelope

`src/commands/improve/loop-stages.ts:745-750` still carries the register's exact wording: "Always 0 since the 0.9.0 confidence-gate deletion; the field stays so the collapse detector's envelope shape is unchanged", and `:831` passes `args.acceptedActions ?? 0` — no caller supplies it. A DIFFERENT `acceptedActions` at improve.ts:1518 is now fed `classCounts.accepted`, which may be why this looked handled. Flagging as unclear: the in-place comment reads as a deliberate keep, but it was never routed or ruled, so the register entry is neither closed nor cancelled.

Files: `src/commands/improve/loop-stages.ts`

### D-16 — add: dangerous-env-key exit code / key count / scan scope

cli.md:42 of the add section now says 'install fails with exit 2 unless --allow-insecure is passed'; add-cli.ts:213 throws a classified error whose exit code I did not run to ground in a sandbox with a poisoned stash. The '23 total' key count no longer appears verbatim. Needs a live repro with a dangerous-key fixture to settle; treat as unverified rather than resolved.

Files: `docs/reference/cli.md`, `src/commands/sources/add-cli.ts`, `src/commands/lint/env-key-rules.ts`

