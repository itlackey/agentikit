# 0.9.2 single-runtime reconstruction brief

This is the recovery ledger for rebuilding pull request #817 around one runtime
architecture. It is both the implementation brief and the status record. Every
phase ends in a recoverable Git commit, and this table is updated with the exact
commit and verification result before the next phase begins.

## Identity and recovery sources

- Reconstruction branch: `codex/reconstruct-0.9.2-final`
- Approved release input: `c5c82c32da4b959c0b7d6ea9554f06a82c2f592e`
- Pull request: `#817` (`release/0.9.2` into `main`)
- Working clone: `.claude/worktrees/codex-reconstruct-final`
- Primary recovery journal:
  `/home/founder3/.codex/sessions/2026/08/17/rollout-2026-08-17T11-02-55-01a01076-2c53-7813-ab6b-e8af90d836c9.jsonl`
- Show-review recovery journal:
  `/home/founder3/.codex/sessions/2026/08/23/rollout-2026-08-23T11-24-21-01a02f6f-f438-7543-8503-8b9e9511df5f.jsonl`
- Parsed call ledger:
  `/home/founder3/code/github/itlackey/akm/.claude/reconstruct-recovery-calls.json`
- Parsed mutation ledger:
  `/home/founder3/code/github/itlackey/akm/.claude/reconstruct-mutations.txt`

The prior damaged replay and partial temporary replays are evidence only. No
file or commit is promoted from them without replaying the original successful
operation against the approved input and verifying the result.

## Required architecture

1. Legacy task files are accepted only by the explicit v2-to-v3 migrator.
   Normal loading, scheduling, projection, and execution accept task v3 only.
2. Markdown and GitHub-shaped YAML workflow sources compile into one source IR.
   That IR freezes into durable plan v4 and one workflow engine executes it.
3. Durable plan v4 is the only executable or resumable stored workflow plan.
   Pre-v4 plans are rejected; they do not enter a parallel decoder/replayer.
4. Command, task, and workflow execution use one resolver and lowering path.
   Shared lowering replaces superseded dispatchers instead of wrapping them.
5. Compatibility is confined to deliberate input and storage boundaries. It is
   not threaded through the current runtime.
6. Semantic inference uses the normal external `@huggingface/transformers`
   package/build input. There is no copied runtime under `src/vendor`, no
   installed-consumer dependency policy, and no second semantic package tree.
7. A contract boundary is tested once. Distinct fault, security, durability,
   and platform behavior remains covered; duplicated characterization and
   consumer-by-consumer ownership tests are removed.

## Upgrade behavior that remains

- `akm upgrade` remains the ordinary package/binary updater.
- Explicit `akm migrate status`, dry-run, and apply retain the task-v2-to-v3
  conversion path, including fail-closed ambiguous inputs.
- Released additive database migration ledger entries remain recognized and
  current managed databases apply known additive DDL on open.
- A durable-v4 workflow resumes from its frozen plan without rereading source,
  configuration, or index state.

These are product upgrade paths. They do not authorize general compatibility
runtimes, three-database cutover machinery, old config-shape coordinators, or
pre-v4 workflow replay.

## Explicit deletion and replacement ledger

| Superseded surface | Required result |
| --- | --- |
| `src/vendor` Hugging Face/ONNX copy | Delete; resolve the ordinary external dependency |
| Installed-consumer semantic manifest/provenance policy | Delete |
| Runtime task-v2 readers/executors | Delete; explicit migrator owns v2 input |
| Pre-v4 workflow plan execution | Delete; reject at the storage boundary |
| Parallel Markdown/YAML execution paths | Replace with peer parsers feeding one source IR |
| Old command/task/workflow dispatch paths | Replace with shared resolve/prepare/lower/dispatch ownership |
| Config-generation/general migration coordinator | Delete |
| Three-database cutover, backup/restore engine, and crash journal | Delete |
| Legacy ref/content/source/task-target migration modules | Delete |
| Legacy proposal/session/persisted-id projection bridges | Delete unless a current storage invariant proves ownership |
| Characterization/meta-analyzer/consumer-copy test suites | Delete or consolidate at the owning boundary |

Every deletion is checked for callers, documentation, package closure, and a
remaining owner. A green test alone is not evidence that two architectures are
acceptable.

## Pull-request review fixes that must survive reconstruction

- `show` renders only indexed entries; physical ownership may arbitrate source
  identity but cannot become a content fallback.
- Frozen standalone scripts preserve true entry semantics for guarded `.js`
  and `.ts` scripts.
- A leading bare `akm` in a task shell command is bound safely to the current
  installation without rewriting explicit paths.
- Scheduler-originated nested command/agent runs preserve the closed five-key
  AKM directory context without reopening arbitrary runtime environment
  override.

## Replay rules

- Replay only operations whose original tool result succeeded.
- Preserve original timestamp order within each phase.
- Replay dynamic `apply_patch` programs, not merely literal patch headers.
- Include the recorded mechanical renames/import rewrites, lock helper rename,
  and task-v2 fixture restoration at their original positions.
- Exclude temporary diagnostics, generated build output, failed patches, and
  ephemeral `.codex` progress artifacts.
- If a patch no longer applies, stop and classify it; do not force a fuzzy or
  guessed edit.
- Record the exact resulting commit and gates here before continuing.

## Checkpoint plan and live status

| Phase | Scope | Status | Recovery commit | Verification / blocker |
| --- | --- | --- | --- | --- |
| 0 | Brief, exact base, recovery inputs | In progress | — | Fresh no-hardlinks clone at exact approved input |
| 1 | External semantic dependency and task-v3 runtime boundary | Pending | — | — |
| 2 | Peer workflow sources, one source IR, durable-v4-only engine | Pending | — | — |
| 3 | Shared execution lowering; delete config/storage/general migration compatibility | Pending | — | — |
| 4 | Apply and independently verify all four PR review fixes | Pending | — | — |
| 5 | Post-review cleanup, docs, ownership/deletion audit, duplicate-test consolidation | Pending | — | — |
| 6 | Install/schema generation, formatting, architecture and diff audit | Pending | — | — |
| 7 | Full unit/integration/package/release gates and remediation | Pending | — | — |
| 8 | Immutable candidate review and PR update | Pending | — | No push until exact candidate is independently approved |

## Required final evidence

- `src/vendor` absent and package closure resolves semantic dependencies through
  the declared external package.
- Searches for task v2, pre-v4 workflow execution, old dispatchers, migration
  coordinators, and retired aliases have an explicit zero-hit or justified
  boundary ledger.
- Source and test diff-size breakdown versus `origin/main`, including the exact
  remaining net additions and the largest owning modules.
- Focused task migration/runtime, source-IR/workflow-v4, shared lowering,
  storage ledger, semantic package, and PR-comment regression suites.
- Biome write pass on touched source/tests/scripts, lint, TypeScript, build,
  `check:changed`, full unit, full integration, package acceptance, and release
  check on one immutable SHA.
- Clean tree, no `dist/tests`, verified bundle, and independent review with no
  unresolved P1/P2 findings before the release branch or PR is updated.

