{
  "configVersion": "0.0.1",
  "defaultEngine": "fast",
  "engines": {
    "fast": {
      "kind": "llm",
      "endpoint": "https://example.test/v1/chat/completions",
      "model": "test-model",
      "apiKey": "${FAST_API_KEY}"
    }
  }
}
