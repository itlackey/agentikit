version: 3
name: v3 to v4 fixture - execution controls hoist
run: akm index --full
shell: bash
env:
  MODE: safe
  RETRIES: 3
akm:
  schedule: "15 4 * * 1"
  engine: fixture-engine
  model: fixture-model
  timeout: 45000
  redact: [CONTRACT_FIXTURE_TOKEN]
  maxSteps: 8
  maxRetries: 2
