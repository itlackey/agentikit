name: Child workflow with declared outputs
on:
  workflow_dispatch:
jobs:
  main:
    runs-on: [self-hosted]
    steps:
      - id: dispatch
        uses: workflows/exporter
      - id: consume
        uses: tasks/consume
        with:
          note:
            from: steps.dispatch.output.report
