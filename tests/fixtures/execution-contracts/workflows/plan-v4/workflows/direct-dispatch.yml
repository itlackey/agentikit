name: Plan v4 direct dispatch
on:
  workflow_dispatch:
jobs:
  main:
    runs-on: [self-hosted]
    steps:
      - id: notify
        uses: commands/plan-v4-notify
      - id: announce
        run: printf plan-v4-shell-ok
