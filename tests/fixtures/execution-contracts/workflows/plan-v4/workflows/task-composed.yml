name: Plan v4 task composed
on:
  workflow_dispatch:
jobs:
  main:
    runs-on: [self-hosted]
    steps:
      - id: notify-via-task
        uses: tasks/plan-v4-task
      - id: build
        uses: scripts/plan-v4-build.sh
