name: Contract review
on:
  workflow_dispatch:
jobs:
  contract:
    runs-on: [self-hosted]
    steps:
      - id: review
        name: review
        run: printf contract-reviewed
      - id: summarize
        name: summarize
        run: printf contract-summarized
