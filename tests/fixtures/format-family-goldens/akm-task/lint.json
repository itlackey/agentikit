{
  "scenario": "akm-task adapter validate(component, changes, ctx) → Diagnostic[]. One conformant strict-v3 task and one invalid selector-XOR task.",
  "specificationGolden": true,
  "capturedAtHead": null,
  "adapterStatus": "IMPLEMENTED — validation delegates to the canonical strict task-v3 parser.",
  "specSource": "docs/architecture/specs/akm-0.9.0-bundle-adapter-spec.md §6 (task validation column: 'invalid-task-yaml: schedule+enabled+one target'), §2/§5.1 (Diagnostic {file, issue, detail, fixed}).",
  "realWorldSource": [
    "AKM-native strict task-v3 source contract — src/tasks/source-v3.ts and schemas/akm-task.json."
  ],
  "notes": [
    "The invalid-task-yaml rule requires exactly one executable selector (uses XOR run) and exactly one scheduling source. two-targets.yml declares both selectors.",
    "issue-code `invalid-task-yaml` is the spec-named code; the `detail` string is illustrative."
  ],
  "perType": {
    "conformant": {
      "relPath": "nightly-index.yml",
      "type": "task",
      "issues": [],
      "note": "version 3 + uses akm/command + strict with.content + akm.schedule is valid; akm.enabled is optional."
    },
    "invalidTwoTargets": {
      "relPath": "two-targets.yml",
      "type": "task",
      "issues": [
        {
          "file": "two-targets.yml",
          "issue": "invalid-task-yaml",
          "detail": "a task-v3 source must declare exactly one executable selector; two-targets.yml declares both uses and run.",
          "fixed": false
        }
      ],
      "note": "single violation: uses plus run. version and akm.schedule are otherwise valid."
    }
  }
}
