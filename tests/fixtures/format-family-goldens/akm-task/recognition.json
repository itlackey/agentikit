{
  "scenario": "akm-task adapter recognize(component, file) for every file in tests/fixtures/bundles/akm-task/. .yml tasks derive type=task; conceptId strips .yml. Recognition does not gate on validity. SPECIFICATION golden.",
  "specificationGolden": true,
  "capturedAtHead": null,
  "adapterStatus": "IMPLEMENTED — the dedicated akm-task adapter validates strict task-v3 .yml sources through the canonical production parser.",
  "specSource": "docs/architecture/specs/akm-0.9.0-bundle-adapter-spec.md §7 (akm-task adapter row: native task-YAML format, export facet), §6 (task row: '.yml under tasks/ (not OKF markdown)'), §1.3 (conceptId = path − ext).",
  "realWorldSource": [
    "AKM-native format — grounded in the strict task-v3 source contract and the current all-types stash fixture (akm/command with nested akm.schedule)."
  ],
  "notes": [
    "Tasks are AKM-native YAML, NOT OKF markdown concepts. Recognition is extension/placement based; validation is strict task v3.",
    "conceptId strips the .yml extension (mirrors the frozen placement golden's taskSpec).",
    "The invalid task (two-targets.yml) is still RECOGNIZED (type=task) — the invalid-task-yaml violation is a validate()/lint concern (see lint.json), not a recognition gate."
  ],
  "byRelPath": {
    "nightly-index.yml": {
      "recognized": true,
      "adapterId": "akm-task",
      "component": "main",
      "type": "task",
      "conceptId": "nightly-index",
      "ref": "sample-akm-task//nightly-index",
      "name": "nightly-index",
      "derivation": ".yml task → type=task. Conformant task v3: uses akm/command + strict with.content + akm.schedule."
    },
    "two-targets.yml": {
      "recognized": true,
      "adapterId": "akm-task",
      "component": "main",
      "type": "task",
      "conceptId": "two-targets",
      "ref": "sample-akm-task//two-targets",
      "name": "two-targets",
      "derivation": ".yml task → type=task. RECOGNIZED despite declaring uses plus run; the selector-XOR violation is reported by validate()."
    }
  }
}
