{
  "scenario": "akm-workflow adapter placeNew(component, conceptId) → on-disk relative path. One workflow format now (workflow-format-unification). SPECIFICATION golden.",
  "specificationGolden": true,
  "capturedAtHead": null,
  "adapterStatus": "IMPLEMENTED — src/core/adapter/adapters/akm-workflow-adapter.ts.",
  "specSource": "docs/architecture/specs/akm-0.9.0-bundle-adapter-spec.md §2 (placeNew), §6 (workflow row).",
  "realWorldSource": [
    "AKM-native; markdown-only placement now that the YAML program form is gone (workflow-format-unification)"
  ],
  "notes": [
    "Component-root-relative POSIX paths. The workflow placement default is <conceptId>.md; an explicit .md conceptId short-circuits (no-op)."
  ],
  "byType": {
    "workflow": {
      "conceptId": "onboarding",
      "assetPath": "onboarding.md",
      "name": "onboarding",
      "note": "default markdown placement."
    }
  },
  "edgeCases": {
    "explicitMdExtensionIsANoOp": {
      "conceptId": "release.md",
      "assetPath": "release.md",
      "note": "an explicit .md conceptId short-circuits the extension append (already has it)."
    },
    "defaultsToMdWhenNoExtension": {
      "conceptId": "release",
      "assetPath": "release.md",
      "note": "no extension → .md default."
    }
  }
}
