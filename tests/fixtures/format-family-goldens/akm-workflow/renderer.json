{
  "scenario": "presentationFor('workflow') — renderer + action for the workflow markdown form. One format now (workflow-format-unification). SPECIFICATION golden.",
  "specificationGolden": true,
  "capturedAtHead": null,
  "adapterStatus": "IMPLEMENTED — src/core/adapter/adapters/akm-workflow-adapter.ts.",
  "specSource": "docs/architecture/specs/akm-0.9.0-bundle-adapter-spec.md §2 / §3.4; src/core/type-presentation.ts TYPE_PRESENTATION['workflow'] (renderer 'workflow-md', action buildWorkflowAction).",
  "realWorldSource": [
    "AKM-native; workflow-format-unification collapsed the two prior renderers (workflow-md / workflow-program-yaml) into one"
  ],
  "notes": [
    "TYPE_PRESENTATION keys `workflow` to renderer 'workflow-md' with buildWorkflowAction.",
    "action = buildWorkflowAction(ref) = \"Start or resume execution with `akm workflow run '<ref>'`.\" (src/core/type-presentation.ts)."
  ],
  "byRelPath": {
    "release.md": {
      "type": "workflow",
      "ref": "sample-akm-workflow//release",
      "label": "Workflow",
      "renderer": "workflow-md",
      "action": "Start or resume execution with `akm workflow run 'sample-akm-workflow//release'`."
    }
  }
}
