# INVALID (fixture, intentional): declares BOTH `uses` and `run`.
# A task source v4 document must declare exactly one executable selector.
# Everything else is valid so this exercises only the selector-XOR rule.
version: 4
uses: commands/summarize-yesterday
run: "echo hello"
