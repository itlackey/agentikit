---
description: "Set up CI/CD pipeline configuration for the project"
---

Generate CI configuration files based on the project type:

- Detect package manager (npm, bun, pnpm)
- Create GitHub Actions workflow for test, lint, build
- Add caching for dependencies
- Configure deployment steps if requested
